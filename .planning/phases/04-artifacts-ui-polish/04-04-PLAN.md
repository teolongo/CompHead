---
phase: 04-artifacts-ui-polish
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - backend/tests/test_phase4_integration.py
autonomous: false
requirements: [OPS-03]
must_haves:
  truths:
    - "Deployed app passes platform endpoint check with frozen /ask schema."
    - "Artifact URLs on Render resolve to downloadable PDF files."
    - "UI verified on desktop and mobile with no console errors."
  artifacts:
    - path: "backend/tests/test_phase4_integration.py"
      provides: "Smoke tests for UI routes and artifact contract"
  key_links:
    - from: "Render deployment"
      to: "POST /ask and GET /"
      via: "PUBLIC_BASE_URL smoke tests"
      pattern: "PUBLIC_BASE_URL"
---

<objective>
Verify Phase 4 end-to-end on the deployed Render host, run self-test iteration, and confirm submission readiness.

Purpose: OPS-03 requires platform self-test feedback loop; judges need working UI + artifacts on production URL.
Output: Integration smoke tests, DevTools verification evidence, and human checkpoint for self-test/submission.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@DEPLOY.md
@.planning/phases/04-artifacts-ui-polish/04-CONTEXT.md
@.planning/phases/04-artifacts-ui-polish/04-UI-SPEC.md
@.planning/ROADMAP.md

<interfaces>
Deployment target: Render (Railway blocked in prior session). PUBLIC_BASE_URL must match Render service URL.
Endpoint check: platform dashboard validates POST /ask schema, no auth, HTTP 200, latency.
Self-test: platform battery reruns sample + hidden questions; iterate on top failures.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 4 integration smoke tests</name>
  <files>backend/tests/test_phase4_integration.py</files>
  <read_first>
    - backend/tests/test_phase3_contract.py
    - backend/main.py
    - backend/tests/test_phase4_graph.py
    - backend/tests/test_phase4_artifacts.py
  </read_first>
  <action>Create `backend/tests/test_phase4_integration.py` using TestClient: GET / returns 200 text/html; GET /api/graph returns nodes; POST /ask with mocked run_agent or live env returns frozen schema keys. Add test that artifact_url when present starts with http and contains /files/.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_phase4_integration.py tests/test_phase4_graph.py tests/test_phase4_artifacts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All phase4 test modules pass in one pytest invocation.
    - Integration test asserts GET / status 200.
    - Integration test asserts POST /ask response keys: answer, sources, verticale, artifact_url.
  </acceptance_criteria>
  <done>Offline smoke coverage for UI routes and ask contract.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy to Render and smoke-test production</name>
  <files>backend/.env.example, DEPLOY.md</files>
  <read_first>
    - DEPLOY.md
    - backend/.env.example
    - backend/main.py
    - AGENTS.md
  </read_first>
  <action>Confirm Render deployment is current (git push or manual deploy). Verify Render env vars: LLM_BASE_URL, LLM_API_KEY, MODEL, MOCK_API_BASE_URL, MOCK_API_TOKEN, PUBLIC_BASE_URL (must equal Render service URL). Run curl smoke tests against PUBLIC_BASE_URL: GET /health, GET /, GET /api/graph, POST /ask with simple CRM question, POST /ask with Q9 deck question, POST /ask with PDF request. Confirm artifact_url from PDF question is reachable GET 200.</action>
  <verify>
    <automated>cd backend && PUBLIC_BASE_URL="${PUBLIC_BASE_URL:?set Render URL}" && curl -fsS "$PUBLIC_BASE_URL/health" && curl -fsS -o /dev/null -w '%{http_code}' "$PUBLIC_BASE_URL/" | grep -q 200</automated>
  </verify>
  <acceptance_criteria>
    - GET `$PUBLIC_BASE_URL/health` returns `{"status":"ok"}`.
    - GET `$PUBLIC_BASE_URL/` returns HTML containing `Company Brain`.
    - POST `$PUBLIC_BASE_URL/ask` returns HTTP 200 JSON with all four response keys.
    - PDF artifact_url returns HTTP 200 when fetched.
  </acceptance_criteria>
  <done>Production deployment serves UI, graph API, and artifacts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: DevTools UI check and platform self-test</name>
  <files>backend/static/index.html</files>
  <read_first>
    - .planning/phases/04-artifacts-ui-polish/04-UI-SPEC.md
    - .planning/phases/04-artifacts-ui-polish/04-CONTEXT.md
    - SAMPLE_QUESTIONS.md
  </read_first>
  <what-built>Full Company Brain UI with chat, graph, artifacts wired on Render.</what-built>
  <how-to-verify>
    1. Open deployed URL in Chrome DevTools — desktop 1280px: submit a CRM question, confirm answer + sources + graph highlight; zero console errors.
    2. Toggle device toolbar 390px: confirm chat-first layout, graph toggle works, touch targets usable.
    3. Run Q9 sample prompt — inline HTML deck visible.
    4. Run platform endpoint check if not already passing.
    5. Run platform self-test battery; note top 3 failures for follow-up (Q10/Q11 deferred fixes acceptable if documented).
  </how-to-verify>
  <resume-signal>Type `self-test done` with pass/fail summary and Render URL, or paste self-test failure details.</resume-signal>
  <acceptance_criteria>
    - Chrome DevTools shows no uncaught errors on / load (D-22).
    - Mobile viewport chat flow completes successfully.
    - Platform endpoint check passes.
    - Self-test executed at least once with results recorded in SUMMARY.
  </acceptance_criteria>
  <done>Phase 4 verified for jury demo and submission.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Render env | Secrets in Render dashboard only |
| public artifact URLs | PDFs world-readable by design |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | PUBLIC_BASE_URL misconfig | mitigate | Set PUBLIC_BASE_URL to exact Render URL before artifact tests |
</threat_model>

<verification>
`cd backend && uv run pytest tests/test_phase4_*.py -q`
Production curls against PUBLIC_BASE_URL for /health, /, /api/graph, /ask, artifact file.
Human: platform self-test + DevTools checklist from 04-UI-SPEC.md.
</verification>

<success_criteria>
- OPS-03: Self-test loop executed with iteration notes.
- Phase 4 ROADMAP success criteria 1-5 addressed (HTML deck, PDF, UI+graph, self-test, submission URL ready).
</success_criteria>

<output>
Create `.planning/phases/04-artifacts-ui-polish/04-04-SUMMARY.md` when done.
</output>
