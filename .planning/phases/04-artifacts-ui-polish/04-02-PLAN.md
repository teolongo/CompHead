---
phase: 04-artifacts-ui-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/agent/artifacts.py
  - backend/agent/loop.py
  - backend/tests/test_phase4_artifacts.py
autonomous: true
requirements: [ART-01, ART-02, ART-03]
must_haves:
  truths:
    - "Q9-style HTML deck questions return inline HTML in answer with artifact_url null."
    - "PDF generation questions save a file under static/files/ and return absolute artifact_url."
    - "artifact_url uses PUBLIC_BASE_URL, not localhost, when env is set."
  artifacts:
    - path: "backend/agent/artifacts.py"
      provides: "Deterministic HTML deck and PDF preflight"
    - path: "backend/agent/loop.py"
      provides: "Artifact preflight wired before LLM loop"
    - path: "backend/tests/test_phase4_artifacts.py"
      provides: "Offline tests for Q9 and PDF paths"
  key_links:
    - from: "backend/agent/loop.py"
      to: "backend/agent/artifacts.py"
      via: "try_answer_artifact_preflight(question)"
      pattern: "try_answer_artifact_preflight"
    - from: "backend/agent/artifacts.py"
      to: "backend/static/files/"
      via: "FPDF.output and uuid filename"
      pattern: "/files/"
---

<objective>
Wire deterministic artifact generation into the agent loop: inline HTML decks for Q9 and PDF binary files with absolute artifact_url, using real CRM/ERP data.

Purpose: ART-01..03 are L2-scored deliverables; preflight avoids LLM latency and ensures factual deck content.
Output: Enabled fpdf2 dependency, loop integration, and tests for deck + PDF paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@SAMPLE_QUESTIONS.md
@.planning/phases/04-artifacts-ui-polish/04-CONTEXT.md
@backend/agent/artifacts.py
@backend/agent/loop.py
@backend/pyproject.toml

<interfaces>
Q9 question (SAMPLE_QUESTIONS.md #9): "Generate a 4-slide HTML deck for the sales rep visiting Primato Supermercati S.p.A. (CUST-0132): profile, open deals, order/lot status, recent call complaints."
Expected: inline HTML in `answer`, sources include crm/* and calls, verticale `crm`, artifact_url null.
PDF path: `_build_pdf_report` writes to `backend/static/files/report-{uuid}.pdf`, returns url `{PUBLIC_BASE_URL}/files/{filename}`.
Preflight order in run_agent: correctness preflight first, then artifact preflight, then LLM loop.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable fpdf2 dependency</name>
  <files>backend/pyproject.toml</files>
  <read_first>
    - backend/pyproject.toml
    - backend/agent/artifacts.py
  </read_first>
  <action>Uncomment `"fpdf2>=2.8.7"` in `backend/pyproject.toml` dependencies. Run `cd backend && uv sync` to install. Do not uncomment other artifact libraries unless needed.</action>
  <verify>
    <automated>cd backend && uv run python -c "from fpdf import FPDF; print(FPDF)"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` lists `fpdf2>=2.8.7` as an active dependency (not commented).
    - `uv run python -c "from fpdf import FPDF"` exits 0.
  </acceptance_criteria>
  <done>PDF generation library is installed and importable.</done>
</task>

<task type="auto">
  <name>Task 2: Wire artifact preflight into agent loop</name>
  <files>backend/agent/loop.py, backend/agent/artifacts.py</files>
  <read_first>
    - backend/agent/loop.py
    - backend/agent/artifacts.py
    - backend/agent/correctness.py
    - AGENTS.md
  </read_first>
  <action>Import `try_answer_artifact_preflight` from `agent.artifacts`. In `run_agent`, after `try_answer_correctness_preflight` returns None, call artifact preflight and return its dict when non-None. Ensure returned dict always has keys `answer`, `sources`, `verticale`, `artifact_url`. Improve `_find_customer_id` matching to detect customer names like "Primato Supermercati" via `/crm/customers?search=` when no CUST-#### token is present, mapping to CUST-0132 for Q9.</action>
  <verify>
    <automated>cd backend && uv run python -c "from agent.loop import run_agent; from unittest.mock import patch; print('import ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `run_agent` calls `try_answer_artifact_preflight` after correctness preflight.
    - Artifact preflight return bypasses the LLM loop entirely.
    - Q9 question text containing `CUST-0132` triggers HTML deck path without LLM when mock API is available.
    - POST /ask schema unchanged.
  </acceptance_criteria>
  <done>Artifact questions are answered deterministically before the LLM loop.</done>
</task>

<task type="auto">
  <name>Task 3: Add artifact tests for Q9 and PDF</name>
  <files>backend/tests/test_phase4_artifacts.py</files>
  <read_first>
    - backend/tests/test_sample_questions.py
    - backend/agent/artifacts.py
    - SAMPLE_QUESTIONS.md
    - backend/agent/loop.py
  </read_first>
  <action>Create `backend/tests/test_phase4_artifacts.py` with: (1) unit test for `_is_html_deck_question` and `_build_html_deck` structure using mocked CRM data — assert answer contains `<section class="slide"` four times; (2) unit test for PDF path with mocked inventory — assert file created under `static/files/` and `artifact_url` starts with configured PUBLIC_BASE_URL; (3) optional integration test `test_sample_question[q09_crm_html_deck]` if sample test id exists or add equivalent live test gated on env vars.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_phase4_artifacts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Offline mocked tests pass without LLM_API_KEY.
    - HTML deck test asserts exactly 4 slide sections in generated HTML.
    - PDF test asserts `artifact_url` matches pattern `{PUBLIC_BASE_URL}/files/report-*.pdf` or mocked base.
    - Generated PDF file exists on disk after test when not mocked.
  </acceptance_criteria>
  <done>Artifact generation behavior is regression-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artifact files on disk | PDFs written to static/files/ are publicly served at /files/ |
| PUBLIC_BASE_URL | Must point to deployed host for evaluator artifact links |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | static/files/ | accept | Challenge requires public artifact URLs |
| T-04-04 | Spoofing | artifact_url | mitigate | Build URL only from PUBLIC_BASE_URL env + generated filename |
</threat_model>

<verification>
`cd backend && uv run pytest tests/test_phase4_artifacts.py -q`
Manual: `curl -X POST localhost:8000/ask -H 'Content-Type: application/json' -d '{"question":"Generate a 4-slide HTML deck for CUST-0132"}'` returns HTML in answer.
</verification>

<success_criteria>
- ART-01: Q9 returns inline HTML deck in answer field.
- ART-02: PDF saved under static/files/.
- ART-03: artifact_url is absolute using PUBLIC_BASE_URL.
</success_criteria>

<output>
Create `.planning/phases/04-artifacts-ui-polish/04-02-SUMMARY.md` when done.
</output>
