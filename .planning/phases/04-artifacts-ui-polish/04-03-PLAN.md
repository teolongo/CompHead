---
phase: 04-artifacts-ui-polish
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - backend/static/index.html
autonomous: true
requirements: [UI-01, UI-02, UI-03]
must_haves:
  truths:
    - "User can submit a question from the browser and see answer, sources, verticale, and artifact_url."
    - "Knowledge graph renders real nodes from GET /api/graph."
    - "Latest /ask response highlights relevant graph nodes and source chips."
  artifacts:
    - path: "backend/static/index.html"
      provides: "Complete Company Brain UI — chat, graph, hero, mobile layout"
  key_links:
    - from: "backend/static/index.html"
      to: "POST /ask"
      via: "fetch('/ask', {method:'POST', body: JSON.stringify({question})})"
      pattern: "fetch('/ask'"
    - from: "backend/static/index.html"
      to: "GET /api/graph"
      via: "fetch('/api/graph')"
      pattern: "fetch('/api/graph'"
---

<objective>
Replace the placeholder page with an award-worthy Company Brain cockpit: chat E2E, interactive knowledge graph, source-path highlighting, ambient Three.js hero, and mobile-friendly layout.

Purpose: UI-01..03 are required L2 deliverables; judges interact with this surface directly.
Output: Single-file `backend/static/index.html` implementing 04-UI-SPEC.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-artifacts-ui-polish/04-CONTEXT.md
@.planning/phases/04-artifacts-ui-polish/04-UI-SPEC.md
@backend/static/index.html
@SAMPLE_QUESTIONS.md

<interfaces>
POST /ask body: `{"question": string}` → `{answer, sources, verticale, artifact_url}`.
GET /api/graph → `{nodes: [{id, label, kind, ...}], edges: [{source, target, relation}], meta}`.
Node kinds for styling: customer, product, supplier, material.
Sample prompts to include as clickable cards: Q1-style CRM, Q9 deck, ERP inventory, KB shelf life.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build page shell, logo, and design tokens</name>
  <files>backend/static/index.html</files>
  <read_first>
    - backend/static/index.html
    - .planning/phases/04-artifacts-ui-polish/04-UI-SPEC.md
    - .planning/phases/04-artifacts-ui-polish/04-CONTEXT.md
  </read_first>
  <action>Replace placeholder with full HTML document. Implement CSS custom properties from UI-SPEC (--bg-base #14120B, --accent-tomato #F54E00, etc.). Add inline SVG logo (pasta strand + neural node). Load Google Fonts (DM Sans + IBM Plex Mono). Structure header, main grid, chat column, graph panel, sample-prompt sidebar. No broken nav links — every button must have a handler or be styled as non-interactive text.</action>
  <verify>
    <automated>test -f backend/static/index.html && grep -q 'Company Brain' backend/static/index.html && grep -q 'var(--bg-base' backend/static/index.html</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/static/index.html` is longer than 200 lines and no longer contains the word `placeholder` in visible UI copy.
    - CSS includes `--accent-tomato: #F54E00` and `--bg-base: #14120B`.
    - Inline SVG logo present in header.
    - Page loads at GET / without 404 for same-origin assets (all JS/CSS inline or CDN).
  </acceptance_criteria>
  <done>Visual foundation and layout grid are in place.</done>
</task>

<task type="auto">
  <name>Task 2: Implement chat flow against POST /ask</name>
  <files>backend/static/index.html</files>
  <read_first>
    - backend/main.py
    - AGENTS.md
    - .planning/phases/04-artifacts-ui-polish/04-UI-SPEC.md
    - backend/static/index.html
  </read_first>
  <action>Add chat thread UI, textarea/input, Ask button, loading state. On submit: POST /ask once (no streaming), append user bubble and assistant bubble. Render answer as innerHTML when it starts with `<!doctype` or `<html` (sandbox in iframe or dedicated panel for deck HTML); otherwise text with pre-wrap. Show source chips from sources[], colored verticale badge, artifact download link when artifact_url is non-null (target=_blank). Include 4+ sample prompt cards that populate the input. Handle errors gracefully (still expect HTTP 200 JSON).</action>
  <verify>
    <automated>grep -q "fetch('/ask'" backend/static/index.html && grep -q 'artifact_url' backend/static/index.html</automated>
  </verify>
  <acceptance_criteria>
    - JavaScript contains `fetch('/ask'` with POST and Content-Type application/json.
    - UI renders `sources`, `verticale`, and `artifact_url` from response JSON.
    - Sample prompt for Q9 deck is present and mentions CUST-0132 or Primato.
    - No Authorization header sent to /ask.
  </acceptance_criteria>
  <done>Chat is end-to-end functional against the frozen /ask contract.</done>
</task>

<task type="auto">
  <name>Task 3: Render knowledge graph with source highlighting</name>
  <files>backend/static/index.html</files>
  <read_first>
    - .planning/phases/04-artifacts-ui-polish/04-UI-SPEC.md
    - backend/services/graph.py
    - backend/static/index.html
  </read_first>
  <action>On load fetch GET /api/graph. Render graph in SVG (force layout via d3-force CDN or deterministic layered positions). Style nodes by kind. Implement highlightGraph(sources, verticale) called after each answer: add CSS class `highlight` to nodes/edges matching source prefixes (crm/ → customers, erp/ → products/materials/suppliers, DOC- → legend KB pulse, calls → customers). Mobile: graph hidden by default behind toggle button "Knowledge graph". Desktop: graph panel visible alongside chat.</action>
  <verify>
    <automated>grep -q "fetch('/api/graph'" backend/static/index.html && grep -q 'highlight' backend/static/index.html</automated>
  </verify>
  <acceptance_criteria>
    - JavaScript fetches `/api/graph` on page load.
    - SVG element renders at least one node circle/rect after successful fetch.
    - Function or logic maps `verticale` and `sources` to highlighted node classes.
    - Mobile media query hides graph until toggle clicked (max-width 768px rule present).
  </acceptance_criteria>
  <done>Interactive graph reflects API data and latest agent sources.</done>
</task>

<task type="auto">
  <name>Task 4: Three.js hero, motion, and reduced-motion fallbacks</name>
  <files>backend/static/index.html</files>
  <read_first>
    - .planning/phases/04-artifacts-ui-polish/04-CONTEXT.md
    - .planning/phases/04-artifacts-ui-polish/04-UI-SPEC.md
  </read_first>
  <action>Add subtle Three.js ambient hero (CDN) with low-poly tube/torus curves suggesting pasta strands. Respect prefers-reduced-motion: skip animation loop and show static gradient/SVG. Optionally add Lenis smooth scroll via CDN; disable on mobile/reduced-motion. Add entrance transitions on chat cards (opacity/transform). Ensure touch targets ≥44px on mobile submit button.</action>
  <verify>
    <automated>grep -q 'three' backend/static/index.html && grep -q 'prefers-reduced-motion' backend/static/index.html</automated>
  </verify>
  <acceptance_criteria>
    - Three.js loaded from CDN and canvas/hero container present.
    - CSS includes `@media (prefers-reduced-motion: reduce)` rule disabling or simplifying animation.
    - Mobile layout CSS present with max-width 768px breakpoint.
    - No console-throwing undefined references on initial load (manual smoke: open / in browser).
  </acceptance_criteria>
  <done>Premium motion layer complete with accessible fallbacks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to /ask | Public endpoint; UI must not add auth |
| inline HTML answers | Deck HTML from own backend only; render in sandboxed iframe when using innerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | XSS | answer rendering | mitigate | Prefer iframe sandbox for full HTML decks; escape user question display |
| T-04-06 | Denial of Service | client fetch | accept | 30s client timeout message; no retry storm |
</threat_model>

<verification>
Manual: start server, open http://localhost:8000/, submit "How many open opportunities for CUST-0132?", verify answer + sources + graph highlight.
Manual: click Q9 sample prompt, verify HTML deck renders.
Chrome DevTools: desktop 1280px and mobile 390px, zero console errors (D-22).
</verification>

<success_criteria>
- UI-01: Working E2E chat against POST /ask.
- UI-02: Graph shows customers, suppliers, products, materials.
- UI-03: Graph data from /api/graph (real API-backed).
</success_criteria>

<output>
Create `.planning/phases/04-artifacts-ui-polish/04-03-SUMMARY.md` when done.
</output>
