---
phase: 04-artifacts-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/graph.py
  - backend/main.py
  - backend/tests/test_phase4_graph.py
autonomous: true
requirements: [UI-02, UI-03]
must_haves:
  truths:
    - "GET /api/graph returns real CRM/ERP nodes and edges without changing POST /ask."
    - "Graph data includes customers, products, suppliers, and raw materials from mock APIs."
    - "BOM nested components[] are flattened into product→material edges."
  artifacts:
    - path: "backend/services/graph.py"
      provides: "Cached graph builder from mock API data"
    - path: "backend/main.py"
      provides: "GET /api/graph UI-only route"
    - path: "backend/tests/test_phase4_graph.py"
      provides: "Contract tests for graph endpoint shape"
  key_links:
    - from: "backend/main.py"
      to: "backend/services/graph.py"
      via: "get_graph_cached()"
      pattern: "/api/graph"
    - from: "backend/services/graph.py"
      to: "services.api_client"
      via: "get_client().get and get_all_pages"
      pattern: "/crm/customers"
---

<objective>
Expose a UI-only knowledge graph API backed by real mock API data so the frontend can render customers, products, suppliers, and materials with accurate relationships.

Purpose: UI-02 and UI-03 require graph data from permitted sources; this plan delivers the backend contract the UI will consume.
Output: `GET /api/graph` returning `{nodes, edges, meta}` with 5-minute cache and unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@API.md
@.planning/phases/04-artifacts-ui-polish/04-CONTEXT.md
@.planning/phases/04-artifacts-ui-polish/04-UI-SPEC.md
@backend/services/graph.py
@backend/main.py
@backend/services/api_client.py

<interfaces>
Graph node shape: `{"id": str, "label": str, "kind": "customer"|"product"|"supplier"|"material", ...meta}`.
Graph edge shape: `{"source": str, "target": str, "relation": str}`.
Node ids use prefixes: `customer:CUST-0132`, `product:PAS-SPA-500`, `supplier:SUP-001`, `material:RAW-SEM-003`.
`get_graph_cached()` returns cached payload for 300s or empty nodes with meta.error on API failure.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize graph builder and BOM flattening</name>
  <files>backend/services/graph.py</files>
  <read_first>
    - backend/services/graph.py
    - backend/services/api_client.py
    - API.md
    - backend/agent/tools/erp.py
  </read_first>
  <action>Ensure `build_graph()` and `_flatten_bom_components()` handle nested ERP BOM rows where `components[]` holds `raw_sku`/`description` entries. Cap fetches with existing MAX_* constants to stay within latency budget. Return `meta` counts for customers, products, suppliers, materials. Keep thread-safe `get_graph_cached()` with 300s TTL and graceful empty fallback on exception.</action>
  <verify>
    <automated>cd backend && uv run python -c "from services.graph import build_graph; g=build_graph(); assert 'nodes' in g and 'edges' in g; print(len(g['nodes']), len(g['edges']))"</automated>
  </verify>
  <acceptance_criteria>
    - `build_graph()` returns dict keys `nodes`, `edges`, `meta`.
    - At least one edge has `relation` value `uses` linking a `product:` node to a `material:` node.
    - `_flatten_bom_components` extracts `raw_sku` from nested `components[]` arrays.
    - No changes to POST /ask route or AskResponse schema.
  </acceptance_criteria>
  <done>Graph builder produces real API-backed nodes and edges including nested BOM components.</done>
</task>

<task type="auto">
  <name>Task 2: Add GET /api/graph route</name>
  <files>backend/main.py</files>
  <read_first>
    - backend/main.py
    - backend/services/graph.py
    - AGENTS.md
  </read_first>
  <action>Add `@app.get("/api/graph")` returning JSON from `get_graph_cached()`. Route is public (no auth), read-only, and not part of the frozen `/ask` contract. Keep existing `/`, `/health`, `/files`, and `POST /ask` unchanged.</action>
  <verify>
    <automated>cd backend && uv run python -c "from fastapi.testclient import TestClient; from main import app; c=TestClient(app); r=c.get('/api/graph'); assert r.status_code==200; assert 'nodes' in r.json()"</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/graph` returns HTTP 200 with JSON body containing `nodes` and `edges` arrays.
    - `POST /ask` path, method, and response model are unchanged.
    - Route does not require Authorization header.
  </acceptance_criteria>
  <done>Frontend can fetch graph data from a dedicated UI-only endpoint.</done>
</task>

<task type="auto">
  <name>Task 3: Add graph endpoint contract tests</name>
  <files>backend/tests/test_phase4_graph.py</files>
  <read_first>
    - backend/tests/test_phase3_contract.py
    - backend/main.py
    - backend/services/graph.py
  </read_first>
  <action>Create `backend/tests/test_phase4_graph.py` with mocked `get_graph_cached` returning sample nodes/edges and asserting response shape via TestClient. Add one test that validates node `kind` values are among customer, product, supplier, material when live env is unavailable (mock only).</action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_phase4_graph.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Tests pass without live MOCK_API_TOKEN when mocks are used.
    - Test asserts `/api/graph` status 200 and top-level keys `nodes`, `edges`.
    - Test file exists at `backend/tests/test_phase4_graph.py`.
  </acceptance_criteria>
  <done>Graph API has offline contract coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet to /api/graph | Same no-auth surface as static UI; read-only aggregated data |
| graph builder to mock API | Uses server-side MOCK_API_TOKEN; token never exposed to browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | /api/graph | accept | Only mock API data already reachable via agent tools |
| T-04-02 | Denial of Service | graph cache | mitigate | 300s TTL cache; capped MAX_* fetch limits |
</threat_model>

<verification>
`cd backend && uv run pytest tests/test_phase4_graph.py -q`
`cd backend && uv run python -c "from fastapi.testclient import TestClient; from main import app; print(TestClient(app).get('/api/graph').json()['meta'])"`
</verification>

<success_criteria>
- UI-02: Graph API returns node types for customers, suppliers, products, materials.
- UI-03: Edges reflect real BOM and supplier relationships from mock APIs.
</success_criteria>

<output>
Create `.planning/phases/04-artifacts-ui-polish/04-01-SUMMARY.md` when done.
</output>
