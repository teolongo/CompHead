---
phase: 01-agent-skeleton-first-answer
plan: 01-01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/.env.example
  - backend/services/__init__.py
  - backend/services/api_client.py
  - backend/services/llm_client.py
  - backend/services/config.py
autonomous: true
requirements:
  - AGENT-05
  - AGENT-06
user_setup:
  - Copy backend/.env.example to backend/.env and fill LLM_API_KEY, MODEL, MOCK_API_TOKEN

must_haves:
  truths:
    - "backend/.env exists with LLM_API_KEY, LLM_BASE_URL, MODEL, MOCK_API_BASE_URL, MOCK_API_TOKEN set"
    - "Mock API client sends Authorization Bearer header on every request"
    - "LLM client uses OpenAI SDK with base_url and api_key from environment"
    - "uv run uvicorn main:app starts without import errors"
  artifacts:
    - path: backend/services/api_client.py
      provides: Authenticated HTTP client for Al Dente mock APIs
      contains: Authorization
    - path: backend/services/llm_client.py
      provides: OpenAI-compatible LLM client factory
      contains: OpenAI
    - path: backend/services/config.py
      provides: Validated settings from environment
      contains: MOCK_API_TOKEN
  key_links:
    - from: backend/services/api_client.py
      to: os.environ MOCK_API_TOKEN
      via: config module
      pattern: Bearer
---

<objective>
Create shared service layer: validated config, authenticated mock-API client, and LLM client factory.

Purpose: Foundation every agent tool and the loop depend on.
Output: Importable services ready for agent tools in Plan 01-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@API.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-agent-skeleton-first-answer/SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config module with required env validation</name>
  <files>backend/services/__init__.py, backend/services/config.py</files>
  <read_first>backend/.env.example, AGENTS.md, backend/main.py</read_first>
  <action>
Create backend/services/ package.

In config.py expose a Settings dataclass or simple functions reading:
- LLM_BASE_URL (default https://api.regolo.ai/v1)
- LLM_API_KEY (required)
- MODEL (required)
- MOCK_API_BASE_URL (default https://aldente.yellowtest.it)
- MOCK_API_TOKEN (required)
- PUBLIC_BASE_URL (default http://localhost:8000)

Add get_settings() that raises a clear ValueError naming the missing variable if any required key is empty.

Do not hardcode secrets. Do not commit .env.
  </action>
  <verify>cd backend && uv run python -c "from services.config import get_settings; s=get_settings(); assert s.MOCK_API_BASE_URL"</verify>
  <acceptance_criteria>
    - backend/services/config.py exists
    - get_settings() reads LLM_API_KEY, MODEL, MOCK_API_TOKEN from environment
    - Missing MOCK_API_TOKEN raises ValueError with variable name in message
    - backend/services/__init__.py exists
  </acceptance_criteria>
  <done>Config module validates all required environment variables</done>
</task>

<task type="auto">
  <name>Task 2: Implement authenticated mock API client</name>
  <files>backend/services/api_client.py</files>
  <read_first>API.md, backend/services/config.py, AGENTS.md</read_first>
  <action>
Create MockApiClient class using httpx.Client (sync is fine for Phase 1).

Methods:
- __init__(self, settings) — store base_url and token
- get(self, path: str, params: dict | None = None) -> dict — GET with Authorization Bearer header, raise on non-2xx with response text, return parsed JSON

Path examples: "/crm/opportunities", "/crm/customers/CUST-0132"

Include a module-level helper get_client() that uses get_settings().

Do NOT implement full pagination helper yet — single-page GET is enough for Plan 01-02 smoke test.
  </action>
  <verify>cd backend && uv run python -c "from services.api_client import get_client; c=get_client(); r=c.get('/health'); print(r)"</verify>
  <acceptance_criteria>
    - backend/services/api_client.py contains class MockApiClient
    - get() sends header Authorization: Bearer {token}
    - get('/health') returns dict with status key when MOCK_API_TOKEN is valid
    - HTTP 401 from API raises an exception with access_denied context
  </acceptance_criteria>
  <done>Mock API client performs authenticated GET requests</done>
</task>

<task type="auto">
  <name>Task 3: Implement LLM client factory</name>
  <files>backend/services/llm_client.py</files>
  <read_first>AGENTS.md, backend/services/config.py</read_first>
  <action>
Create get_llm_client() returning openai.OpenAI(api_key=settings.LLM_API_KEY, base_url=settings.LLM_BASE_URL).

Create get_model() returning settings.MODEL string.

Add brief docstring noting Regolo model ids are case-sensitive and tool-calling model is required.

No agent loop in this file — client factory only.
  </action>
  <verify>cd backend && uv run python -c "from services.llm_client import get_llm_client, get_model; c=get_llm_client(); m=get_model(); print(m)"</verify>
  <acceptance_criteria>
    - backend/services/llm_client.py imports OpenAI from openai package
    - get_llm_client() uses LLM_BASE_URL and LLM_API_KEY from get_settings()
    - get_model() returns MODEL env value
  </acceptance_criteria>
  <done>LLM client factory ready for agent loop</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] backend/.env exists with real keys (user setup)
- [ ] uv run python imports all services modules without error
- [ ] Mock API health check succeeds with valid token
</verification>

<success_criteria>
- All tasks completed
- Service layer ready for agent tools
- No changes to POST /ask yet (Plan 01-02)
</success_criteria>
