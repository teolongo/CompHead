---
phase: 02-all-4-verticali-kb
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - backend/agent/tools/crm.py
  - backend/agent/tools/__init__.py
  - backend/agent/loop.py
  - backend/agent/prompts.py
  - backend/tests/test_phase2_q1_q4.py
autonomous: true
requirements: [CRM-01]
user_setup: []

must_haves:
  truths:
    - "D-13: verticale extracted from submit_answer tool — not hardcoded crm"
    - "D-14: CRM mixed granularity — list_opportunities with pre-computation; thin list tools for customers/orders/invoices"
    - "D-15: SYSTEM_PROMPT contains per-verticale routing hints for all four domains"
    - "D-16: MAX_ITERATIONS increased to 8 for two-step calls workflows"
    - "Agent answers Q1: CUST-0132 has 4 open opportunities worth 740,000 EUR total"
    - "CRM tools cover customers, opportunities, orders, invoices with filters"
    - "All four verticali Q1-Q4 pass in single integration test battery"
  artifacts:
    - path: backend/agent/loop.py
      provides: "verticale extraction and MAX_ITERATIONS=8"
      contains: "MAX_ITERATIONS = 8"
    - path: backend/agent/tools/crm.py
      provides: "Expanded CRM tool set"
      exports: ["get_tool_definitions", "run_crm_tool"]
    - path: backend/tests/test_phase2_q1_q4.py
      provides: "Q1-Q4 integration test battery"
  key_links:
    - from: backend/agent/loop.py
      to: submit_answer or verticale parser
      via: "final answer extraction per D-13"
      pattern: "verticale"
    - from: backend/agent/prompts.py
      to: all tool modules
      via: "per-verticale routing hints D-15"
      pattern: "crm|erp|calls|kb"
---

## Phase Goal

**As a** company brain user, **I want to** ask any CRM/ERP/calls/KB question and get answers with the correct verticale tag, **so that** the evaluator and UI know which data source dominated.

<objective>
Complete Phase 2: expand CRM tools, implement reliable verticale extraction (D-13), finalize routing prompts, bump MAX_ITERATIONS (D-16), verify Q1-Q4 all pass.

Purpose: Q1 validates CRM expansion; D-13 fixes hardcoded verticale; integration battery proves phase goal.
Output: expanded crm.py, loop.py verticale mechanism, final prompts, Q1-Q4 test battery.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-all-4-verticali-kb/02-CONTEXT.md
@.planning/phases/02-all-4-verticali-kb/02-PATTERNS.md
@SAMPLE_QUESTIONS.md
@API.md

<interfaces>
From backend/agent/loop.py (current):
- verticale hardcoded to "crm" at lines 32 and 101 — must replace per D-13
- MAX_ITERATIONS = 5 — increase to 8 per D-16

From backend/agent/tools/crm.py (current):
- Only list_opportunities exists; CRM-01 requires customers, orders, invoices too

API.md CRM endpoints:
- GET /crm/customers — search, channel, region filters
- GET /crm/opportunities — customer_id, stage
- GET /crm/orders — customer_id, status, from, to
- GET /crm/invoices — customer_id, status, from, to

Q1 reference: CUST-0132, 4 open opportunities, 740,000 EUR total
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing Q1-Q4 integration test battery</name>
  <files>backend/tests/test_phase2_q1_q4.py</files>
  <read_first>SAMPLE_QUESTIONS.md Q1-Q4, backend/agent/loop.py, 02-CONTEXT.md</read_first>
  <acceptance_criteria>
    - Parametrized integration tests for Q1, Q2, Q3, Q4 question texts
    - Each test asserts answer contains key facts from SAMPLE_QUESTIONS.md reference answers
    - Each test asserts verticale matches expected domain (crm, erp, calls, kb)
    - Each test asserts sources non-empty with expected prefix (crm/, erp/, calls/, DOC-)
    - Tests skip when MOCK_API_TOKEN or LLM_API_KEY unset
    - Q1 test fails on verticale=crm assertion if verticale still hardcoded for non-CRM questions (or Q1-specific assertions fail before CRM expansion)
  </acceptance_criteria>
  <behavior>
    - Q1: answer has 4, 740,000, verticale=crm
    - Q2: answer has below minimum and 462, verticale=erp
    - Q3: answer has broken pasta and LOT-2026-0658, verticale=calls
    - Q4: answer has 36 months and gluten, verticale=kb
  </behavior>
  <action>
    Create backend/tests/test_phase2_q1_q4.py with pytest.mark.integration and pytest.mark.parametrize over four (question, expected_verticale, expected_substrings) tuples from SAMPLE_QUESTIONS.md.
    Call run_agent(question) and assert response dict fields.
    Use generous substring matching (case-insensitive) for numeric and keyword facts.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_phase2_q1_q4.py -x 2>&1 | grep -E "FAILED|SKIPPED|passed" | head -3</automated>
  </verify>
  <done>Test battery exists; fails on verticale or missing CRM tools until Tasks 2-3 complete</done>
</task>

<task type="auto">
  <name>Task 2: Expand CRM tools for customers, orders, invoices</name>
  <files>backend/agent/tools/crm.py</files>
  <read_first>backend/agent/tools/crm.py, API.md CRM section, 02-CONTEXT.md D-01, D-03, D-14</read_first>
  <acceptance_criteria>
    - CRM module has four tools: list_opportunities (existing), list_customers, list_orders, list_invoices
    - Each tool follows pre-computed summary pattern with Python-side counts/totals where applicable
    - Sample rows capped at 100 per D-03
    - Source strings: crm/customers, crm/opportunities, crm/orders, crm/invoices
    - list_opportunities unchanged behavior for Q1 (open = qualification + negotiation)
    - Q1 unit test: list_opportunities for CUST-0132 returns count=4, total_value_eur=740000
  </acceptance_criteria>
  <action>
    Extend backend/agent/tools/crm.py per D-14 mixed granularity:
    - list_customers: GET /crm/customers with search, channel, region filters; return count + sample
    - list_orders: GET /crm/orders with customer_id, status, from, to; return count + total_value if applicable + sample
    - list_invoices: GET /crm/invoices with customer_id, status, from, to; return count + total_amount + sample
    - Keep list_opportunities as-is for Q1 compatibility
    - run_crm_tool dispatches all four by name
    Add unit test in test_phase2_q1_q4.py or separate test_crm.py for list_opportunities mocked Q1 values.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/ -k "opportunities or q1" -x -q</automated>
  </verify>
  <done>CRM-01 satisfied with four endpoint tools; Q1 unit assertions pass</done>
</task>

<task type="auto">
  <name>Task 3: Verticale extraction, MAX_ITERATIONS, and final routing prompts</name>
  <files>backend/agent/loop.py, backend/agent/prompts.py, backend/agent/tools/__init__.py</files>
  <read_first>backend/agent/loop.py, backend/agent/prompts.py, 02-CONTEXT.md D-13, D-15, D-16</read_first>
  <acceptance_criteria>
    - MAX_ITERATIONS increased to 8 per D-16
    - verticale no longer hardcoded — extracted reliably per D-13
    - Mechanism: add submit_answer tool with required verticale (enum crm|erp|calls|kb) and answer (string) fields; loop treats submit_answer as terminal — uses its fields for response
    - Fallback: if LLM responds without submit_answer, infer verticale from sources prefixes (crm/→crm, erp/→erp, calls/→calls, DOC-→kb) with crm default only if no sources
    - SYSTEM_PROMPT instructs LLM to always finish by calling submit_answer with correct verticale
    - SYSTEM_PROMPT contains complete per-verticale routing for all four domains per D-15
    - Q1-Q4 integration battery passes when .env configured
  </acceptance_criteria>
  <action>
    Add submit_answer to a new tool definition in backend/agent/tools/__init__.py (or a small agent/tools/answer.py) — prefer centralizing in __init__.py FINAL_TOOLS list merged at end of get_tool_definitions().
    submit_answer parameters: answer (string, required), verticale (enum, required).
    run_tool: if name==submit_answer, return json.dumps(arguments), "" (no source added).
    Update loop.py:
    - MAX_ITERATIONS = 8
    - In tool_calls loop: if any call is submit_answer, parse arguments for answer+verticale, set as final response, break
    - After loop: if no submit_answer, use _extract_message_text for answer and _infer_verticale(sources) helper
    - Remove hardcoded verticale = "crm" initialization default for success path
    Rewrite SYSTEM_PROMPT with four verticale sections (crm, erp, calls, kb) per D-15 and explicit instruction to call submit_answer as final step with dominant verticale.
    Run full test battery.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_phase2_q1_q4.py tests/test_erp_q2.py tests/test_calls_q3.py tests/test_kb_q4.py -q</automated>
  </verify>
  <done>All four Q1-Q4 integration tests pass; verticale correctly set per question domain</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → submit_answer | Untrusted verticale enum — must validate |
| LLM → CRM filters | Untrusted filter values to mock API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | verticale value | mitigate | Validate against allowed set {crm,erp,calls,kb}; fallback to sources inference |
| T-02-09 | Elevation | submit_answer bypass | accept | Public /ask endpoint; no auth required by spec |
| T-02-10 | Denial of Service | iteration exhaustion | mitigate | MAX_ITERATIONS=8 cap; 30s httpx timeout on API calls |
| T-02-SC | Tampering | package installs | accept | No new packages in this plan |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/ -q` all phase 2 tests pass
- Manual: curl all four SAMPLE_QUESTIONS Q1-Q4; each returns correct verticale and key facts
- POST /ask schema unchanged; always HTTP 200
</verification>

<success_criteria>
- CRM-01 satisfied alongside ERP-01, CALLS-01, KB-01 from prior plans
- D-13: verticale from LLM declaration, not hardcoded
- D-15, D-16 implemented
- Phase 2 goal met: Q1-Q4 pass locally
</success_criteria>

<output>
Create `.planning/phases/02-all-4-verticali-kb/02-04-SUMMARY.md` when done
</output>
