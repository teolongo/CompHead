---
phase: 02-all-4-verticali-kb
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - backend/pyproject.toml
  - backend/agent/tools/kb.py
  - backend/agent/tools/__init__.py
  - backend/agent/prompts.py
  - backend/tests/test_kb_q4.py
autonomous: false
requirements: [KB-01]
user_setup: []

must_haves:
  truths:
    - "Agent answers Q4: PAS-SPA-500 shelf life 36 months, allergens gluten, may contain soy and mustard"
    - "KB tool returns full matched document text (no chunking)"
    - "Source reported as DOC-001 (document ID, not file path)"
    - "SKU exact match attempted before BM25 fallback"
  artifacts:
    - path: backend/agent/tools/kb.py
      provides: "search_kb tool with SKU scan + BM25"
      exports: ["get_tool_definitions", "run_kb_tool"]
    - path: backend/pyproject.toml
      provides: "rank-bm25 dependency active"
  key_links:
    - from: backend/agent/tools/kb.py
      to: backend/data/kb/
      via: "Path glob DOC-*.md"
      pattern: "data/kb"
    - from: backend/agent/tools/kb.py
      to: rank_bm25
      via: "BM25Okapi index over full doc bodies"
      pattern: "BM25Okapi"
---

## Phase Goal

**As a** company brain user, **I want to** ask product spec questions by SKU, **so that** I get accurate shelf life and allergen data from the knowledge base.

<objective>
Deliver the KB vertical slice end-to-end: two-stage retrieval (SKU exact + BM25 fallback), whole-document return, barrel wiring, Q4 passing locally.

Purpose: Q4 is the Phase 2 KB success gate; adds rank-bm25 dependency per D-08.
Output: `kb.py`, pyproject.toml update, Q4 test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-all-4-verticali-kb/02-CONTEXT.md
@.planning/phases/02-all-4-verticali-kb/02-PATTERNS.md
@backend/data/kb/DOC-001.md
@SAMPLE_QUESTIONS.md

<interfaces>
From backend/agent/tools/__init__.py (after 02-02):
- Barrel aggregates CRM + ERP + Calls; extend with KB

KB directory: backend/data/kb/DOC-*.md — 35 markdown documents
DOC-001 contains PAS-SPA-500, shelf life 36 months, allergens gluten, may contain soy/mustard

Q4 reference: SKU PAS-SPA-500 → DOC-001, 36 months, gluten (+ may contain soy, mustard)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify rank-bm25 package legitimacy</name>
  <files>backend/pyproject.toml</files>
  <read_first>02-CONTEXT.md D-08, backend/pyproject.toml</read_first>
  <acceptance_criteria>
    - Human confirms rank-bm25 on PyPI is legitimate before uv sync
    - Version >=0.2.2 available
    - Executor may proceed to uncomment dependency only after approval
  </acceptance_criteria>
  <action>
    Pause for human verification of rank-bm25 package on PyPI per Package Legitimacy Gate ([ASSUMED] status — no RESEARCH.md audit table). Executor must not run uv sync with rank-bm25 until human approves.
  </action>
  <what-built>Package legitimacy gate for rank-bm25>=0.2.2 per D-08</what-built>
  <how-to-verify>
    1. Visit https://pypi.org/project/rank-bm25/ — confirm package exists, maintained, matches lexical retrieval use case
    2. Confirm version >=0.2.2 available
    3. Type "approved" to proceed or specify alternative
  </how-to-verify>
  <verify>
    <human-check>User confirms rank-bm25 approved on PyPI</human-check>
  </verify>
  <done>Human approved rank-bm25 install</done>
  <resume-signal>Type "approved" to proceed with rank-bm25 install, or specify alternative</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Failing Q4 tests for KB retrieval</name>
  <files>backend/tests/test_kb_q4.py</files>
  <read_first>backend/data/kb/DOC-001.md, 02-CONTEXT.md D-05, D-06, D-07, SAMPLE_QUESTIONS.md Q4</read_first>
  <acceptance_criteria>
    - Unit test: search_kb with sku PAS-SPA-500 returns document_id DOC-001, match_method sku_exact, full_document_text containing 36 months and gluten
    - Unit test: search_kb with query "returns policy" (no SKU) uses bm25 match_method
    - Source string is DOC-001 not a file path
    - Tests fail before kb.py exists
  </acceptance_criteria>
  <behavior>
    - SKU scan: PAS-SPA-500 in query → DOC-001, match_method=sku_exact
    - BM25 fallback: generic query → top doc by BM25 score, match_method=bm25
    - full_document_text is entire markdown file content
  </behavior>
  <action>
    Create backend/tests/test_kb_q4.py testing run_kb_tool("search_kb", ...) against real files in backend/data/kb/ (no API mock needed).
    Assert document_id, match_method, and content substrings for Q4 facts.
    Do not implement kb.py yet — tests must fail RED.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_kb_q4.py -x 2>&1 | grep -E "FAILED|ERROR|no tests ran" | head -1</automated>
  </verify>
  <done>Test file exists; tests fail because kb module missing</done>
</task>

<task type="auto">
  <name>Task 3: Implement KB tool, wire barrel, and verify Q4 E2E</name>
  <files>backend/pyproject.toml, backend/agent/tools/kb.py, backend/agent/tools/__init__.py, backend/agent/prompts.py, backend/tests/test_kb_q4.py</files>
  <read_first>backend/agent/tools/crm.py, 02-CONTEXT.md D-05, D-06, D-07, D-08, D-15, backend/data/kb/DOC-001.md</read_first>
  <acceptance_criteria>
    - pyproject.toml has rank-bm25>=0.2.2 uncommented; uv sync succeeds (after Task 1 approval)
    - search_kb tool: SKU exact match first, BM25 fallback, full document text, DOC-### sources
    - Barrel includes KB tools; run_tool dispatches to run_kb_tool
    - SYSTEM_PROMPT adds kb routing section per D-15
    - All test_kb_q4.py tests pass including integration
  </acceptance_criteria>
  <action>
    Uncomment rank-bm25 in pyproject.toml; run uv sync.
    Create backend/agent/tools/kb.py with _KB_DIR, _find_by_sku, lazy BM25Okapi index, search_kb tool per D-05/D-06/D-07.
    Extend __init__.py with kb imports and KB_TOOLS dispatch.
    Extend SYSTEM_PROMPT with kb routing per D-15.
    Add integration test to test_kb_q4.py for Q4 via run_agent.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_kb_q4.py -x -q</automated>
  </verify>
  <done>KB vertical slice complete; Q4 tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KB files → tool | Read-only local markdown; no user upload |
| LLM → query args | Untrusted search strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | local KB files | accept | Read-only bundled data; no write path |
| T-02-07 | Denial of Service | full doc text to LLM | accept | 35 small docs; whole-doc retrieval is explicit requirement (D-06) |
| T-02-SC | Tampering | rank-bm25 pip install | mitigate | Human checkpoint Task 1 verifies PyPI package before uv sync |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/test_kb_q4.py -q` passes
- search_kb never returns file paths in sources — only DOC-### IDs
</verification>

<success_criteria>
- KB-01 satisfied: retrieve relevant KB documents by query
- Q4 reference answer achievable via agent loop
- Two-stage SKU-then-BM25 retrieval working
</success_criteria>

<output>
Create `.planning/phases/02-all-4-verticali-kb/02-03-SUMMARY.md` when done
</output>
