---
phase: 02-all-4-verticali-kb
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agent/tools/erp.py
  - backend/agent/tools/__init__.py
  - backend/agent/loop.py
  - backend/agent/prompts.py
  - backend/tests/test_erp_q2.py
autonomous: true
requirements: [ERP-01]
user_setup: []

must_haves:
  truths:
    - "Agent answers Q2: PAS-PEN-500 is below minimum stock with on-hand 462 vs minimum 2000"
    - "ERP inventory tool returns pre-computed below_minimum, on_hand_qty, minimum_qty fields"
    - "POST /ask response includes verticale=erp and source erp/inventory for Q2"
  artifacts:
    - path: backend/agent/tools/erp.py
      provides: "get_inventory tool with Python-side stock comparison"
      exports: ["get_tool_definitions", "run_erp_tool"]
    - path: backend/agent/tools/__init__.py
      provides: "Barrel dispatch for CRM + ERP tools"
      exports: ["get_tool_definitions", "run_tool"]
    - path: backend/tests/test_erp_q2.py
      provides: "Q2 unit and integration test scaffold"
  key_links:
    - from: backend/agent/loop.py
      to: backend/agent/tools/__init__.py
      via: "run_tool dispatch"
      pattern: "from agent.tools import get_tool_definitions, run_tool"
    - from: backend/agent/tools/erp.py
      to: services/api_client.py
      via: "get_client().get('/erp/inventory')"
      pattern: "get_client\\(\\)\\.get\\(\"/erp/inventory\""
---

## Phase Goal

**As a** company brain user, **I want to** ask ERP inventory questions and get accurate stock status, **so that** I know whether SKUs are below minimum without manual ERP lookup.

<objective>
Deliver the ERP vertical slice end-to-end: `get_inventory` tool with pre-computed stock fields, barrel dispatch, loop wiring, and Q2 passing locally.

Purpose: Q2 is the Phase 2 ERP success gate; establishes barrel pattern for calls/KB plans.
Output: `erp.py`, updated `__init__.py` + `loop.py` + `prompts.py`, Q2 test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-all-4-verticali-kb/02-CONTEXT.md
@.planning/phases/02-all-4-verticali-kb/02-PATTERNS.md
@.planning/phases/01-agent-skeleton-first-answer/01-02-SUMMARY.md
@AGENTS.md
@API.md
@SAMPLE_QUESTIONS.md

<interfaces>
From backend/agent/tools/crm.py:
- `get_tool_definitions() -> list[dict[str, Any]]`
- `run_crm_tool(name: str, arguments: dict[str, Any]) -> tuple[str, str]` — returns `(json_result, source_string)`
- Pre-computation pattern: Python computes aggregates; returns explicit fields in JSON; source like `"crm/opportunities"`

From backend/agent/loop.py:
- `run_agent(question: str) -> dict[str, Any]` — keys: answer, sources, verticale, artifact_url
- `_execute_tool(name, arguments) -> tuple[str, str]` — currently CRM-only; replace with barrel `run_tool`
- `MAX_ITERATIONS = 5` (verticale bump deferred to plan 02-04)

From backend/services/api_client.py:
- `get_client().get(path, params=None) -> dict` — pagination envelope `{data: [], pagination: {total, offset, limit}}`

API.md ERP inventory filters: `type` (finished_good/raw_material), `below_min` (true), `search` (SKU substring)
Q2 reference: PAS-PEN-500, on-hand 462, minimum 2000, below minimum = true
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing Q2 tests for ERP inventory tool</name>
  <files>backend/tests/test_erp_q2.py, backend/tests/conftest.py</files>
  <read_first>backend/agent/tools/crm.py, API.md (erp/inventory section), SAMPLE_QUESTIONS.md Q2</read_first>
  <acceptance_criteria>
    - conftest.py exists with pytest path setup for backend imports
    - Unit test mocks get_client().get and asserts get_inventory returns below_minimum=true, on_hand_qty=462, minimum_qty=2000 for PAS-PEN-500
    - Integration test (marked @pytest.mark.integration) posts Q2 to run_agent and asserts answer mentions below minimum and 462; skipped when MOCK_API_TOKEN unset
    - Tests fail before erp.py exists
  </acceptance_criteria>
  <behavior>
    - Unit: mock /erp/inventory row with sku PAS-PEN-500, on_hand_qty 462, minimum_qty 2000 → tool JSON has below_minimum true
    - Integration: Q2 question → answer contains "462" and below-minimum semantics; sources includes erp/inventory
  </behavior>
  <action>
    Create backend/tests/conftest.py adding backend/ to sys.path. Create test_erp_q2.py with:
    (1) unit test calling run_erp_tool("get_inventory", {"search": "PAS-PEN-500"}) with mocked get_client;
    (2) integration test calling run_agent with Q2 text, skipif no env token.
    Use unittest.mock.patch on services.api_client.get_client. Do not implement erp.py yet — tests must fail RED.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_erp_q2.py -x 2>&1 | grep -E "FAILED|ERROR|no tests ran" | head -1</automated>
  </verify>
  <done>Test file exists; pytest collects tests; at least one test fails because erp module missing</done>
</task>

<task type="auto">
  <name>Task 2: Implement get_inventory ERP tool</name>
  <files>backend/agent/tools/erp.py</files>
  <read_first>backend/agent/tools/crm.py, .planning/phases/02-all-4-verticali-kb/02-CONTEXT.md (D-01, D-02, D-03, D-14)</read_first>
  <acceptance_criteria>
    - erp.py exports get_tool_definitions and run_erp_tool matching CRM module pattern
    - get_inventory accepts search (SKU), type, below_min filters per API.md
    - Python computes below_minimum bool, on_hand_qty, minimum_qty from matched row (D-01, D-02)
    - Sample rows capped at 100 (D-03)
    - Returns source string erp/inventory
    - Thin list tools per D-14: list_bom (sku filter), list_suppliers (search, category), list_production_orders (sku, status), list_shipments (customer_id, order_id) — count + sample[:100], no complex pre-computation
    - Unit test from Task 1 passes GREEN
  </acceptance_criteria>
  <action>
    Create backend/agent/tools/erp.py following crm.py exactly:
    - TOOL_DEFINITIONS with get_inventory function schema; description states pre-computed below_minimum, on_hand_qty, minimum_qty fields
    - _compute_inventory_status(row) helper returning (below_minimum: bool, on_hand_qty: int, minimum_qty: int)
    - run_erp_tool dispatches get_inventory: build params from arguments (search, type, below_min), call get_client().get("/erp/inventory", params), find row matching SKU if multiple returned, compute status in Python
    - Result JSON: {sku, below_minimum, on_hand_qty, minimum_qty, inventory_sample: rows[:100]}
    - Return json.dumps(result), "erp/inventory"
    Per D-14: inventory is standalone tool (complex computed fields); add grouped list tools for bom, suppliers, production-orders, shipments as thin pass-through wrappers with count + sample.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_erp_q2.py::test_get_inventory_below_minimum -x -q</automated>
  </verify>
  <done>Unit test GREEN; tool returns explicit below_minimum=true and qty fields for PAS-PEN-500</done>
</task>

<task type="auto">
  <name>Task 3: Wire ERP slice into barrel, loop, and prompts</name>
  <files>backend/agent/tools/__init__.py, backend/agent/loop.py, backend/agent/prompts.py</files>
  <read_first>backend/agent/loop.py, backend/agent/tools/crm.py, .planning/phases/02-all-4-verticali-kb/02-PATTERNS.md</read_first>
  <acceptance_criteria>
    - __init__.py aggregates CRM + ERP tool definitions; run_tool dispatches by tool name to run_crm_tool or run_erp_tool
    - loop.py imports from agent.tools barrel (not agent.tools.crm directly)
    - SYSTEM_PROMPT includes ERP routing hint: use get_inventory for stock/below-minimum questions with SKU search
    - Q2 integration test passes when .env configured; verticale still hardcoded crm until plan 02-04 (acceptable for this slice)
    - sources includes erp/inventory for Q2
  </acceptance_criteria>
  <action>
    Implement barrel in backend/agent/tools/__init__.py:
    - Import get_tool_definitions and run_* from crm and erp
    - get_tool_definitions returns concatenated lists
    - run_tool: if name in CRM_TOOLS set call run_crm_tool else if in ERP_TOOLS call run_erp_tool else raise ValueError
    Update loop.py: replace `from agent.tools.crm import ...` with `from agent.tools import get_tool_definitions, run_tool`; _execute_tool calls run_tool(name, args).
    Extend SYSTEM_PROMPT with erp section per D-15: inventory lookups via get_inventory, mention below_min filter and pre-computed fields.
    Do NOT implement verticale extraction here (D-13 deferred to 02-04). Do NOT change MAX_ITERATIONS yet (D-16 deferred).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_erp_q2.py -x -q</automated>
  </verify>
  <done>Barrel dispatch works; Q2 unit test passes; loop uses unified run_tool</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → tool args | Untrusted filter values passed to mock API |
| Tool → mock API | Authenticated read-only external HTTP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | get_inventory params | mitigate | Only pass whitelisted API.md filter keys; additionalProperties false in schema |
| T-02-02 | Information Disclosure | tool JSON payload | accept | Mock API data only; no secrets in tool results |
| T-02-03 | Denial of Service | inventory row cap | mitigate | Cap sample at 100 rows (D-03); httpx 30s timeout already set |
| T-02-SC | Tampering | uv sync installs | accept | No new packages in this plan |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/test_erp_q2.py -q` all unit tests pass
- Manual smoke (when .env set): curl Q2 from SAMPLE_QUESTIONS.md returns below minimum + 462
</verification>

<success_criteria>
- ERP-01 satisfied: inventory query with filters and pre-computed status
- Q2 reference answer achievable via agent loop
- Barrel pattern established for subsequent verticali plans
</success_criteria>

<output>
Create `.planning/phases/02-all-4-verticali-kb/02-01-SUMMARY.md` when done
</output>
