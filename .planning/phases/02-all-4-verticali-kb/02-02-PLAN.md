---
phase: 02-all-4-verticali-kb
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - backend/agent/tools/calls.py
  - backend/agent/tools/__init__.py
  - backend/agent/prompts.py
  - backend/tests/test_calls_q3.py
autonomous: true
requirements: [CALLS-01]
user_setup: []

must_haves:
  truths:
    - "D-09: Two separate tools — list_calls and search_transcript with ?search="
    - "D-10: list_calls pre-computes most_recent_call_id sorted by date desc"
    - "D-11: search_transcript returns hybrid payload with complaint_type, lot_id, matched_segments"
    - "D-12: Transcript segments capped at 20 — never full transcript download"
    - "Agent answers Q3: NordSpesa last call complaint is broken pasta on lot LOT-2026-0658 (call CALL-58020)"
  artifacts:
    - path: backend/agent/tools/calls.py
      provides: "list_calls and search_transcript tools"
      exports: ["get_tool_definitions", "run_calls_tool"]
    - path: backend/tests/test_calls_q3.py
      provides: "Q3 unit tests for calls workflow"
  key_links:
    - from: backend/agent/tools/calls.py
      to: "/calls and /calls/{id}/transcript"
      via: "get_client().get with search param"
      pattern: "get_client\\(\\)\\.get\\(\"/calls"
    - from: backend/agent/tools/__init__.py
      to: run_calls_tool
      via: "barrel run_tool dispatch"
      pattern: "run_calls_tool"
---

## Phase Goal

**As a** company brain user, **I want to** ask about customer call complaints and lots, **so that** I get accurate call intelligence without downloading full transcripts.

<objective>
Deliver the Calls vertical slice end-to-end: two-tool workflow (list_calls → search_transcript), pre-computed complaint fields, barrel extension, Q3 passing locally.

Purpose: Q3 is the Phase 2 calls success gate; validates two-step agent workflow.
Output: `calls.py`, barrel + prompt updates, Q3 test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-all-4-verticali-kb/02-CONTEXT.md
@.planning/phases/02-all-4-verticali-kb/02-PATTERNS.md
@SAMPLE_QUESTIONS.md
@API.md

<interfaces>
From backend/agent/tools/__init__.py (after 02-01):
- `get_tool_definitions()` — CRM + ERP merged
- `run_tool(name, arguments) -> tuple[str, str]` — extend with calls dispatch

API.md calls endpoints:
- GET /calls — filters: customer_id, type, outcome, from, to
- GET /calls/{id}/transcript — params: search, speaker, offset, limit; response uses `segments` not `data`

Q3 reference: CUST-0137 NordSpesa, call CALL-58020, complaint broken pasta, lot LOT-2026-0658
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing Q3 tests for calls tools</name>
  <files>backend/tests/test_calls_q3.py</files>
  <read_first>backend/agent/tools/crm.py, API.md (calls section), SAMPLE_QUESTIONS.md Q3, 02-CONTEXT.md D-09..D-12</read_first>
  <acceptance_criteria>
    - Unit test: list_calls with customer_id CUST-0137 returns most_recent_call_id CALL-58020 sorted by date desc
    - Unit test: search_transcript with call_id CALL-58020 and search broken returns complaint_type and lot_id LOT-2026-0658
    - Unit test: search_transcript caps matched_segments at 20
    - Integration test for Q3 via run_agent (skipif MOCK_API_TOKEN or LLM_API_KEY unset)
    - Tests fail before calls.py exists
  </acceptance_criteria>
  <behavior>
    - list_calls: mocked rows with different dates → most_recent_call_id is newest
    - search_transcript: mocked segments with "broken pasta" and "LOT-2026-0658" → pre-extracted fields populated
    - Segment list length ≤ 20 even when API returns more
  </behavior>
  <action>
    Create backend/tests/test_calls_q3.py with mocked get_client responses mirroring Q3 entities.
    Test run_calls_tool directly for list_calls and search_transcript.
    Add integration test posting Q3 question to run_agent.
    Use pytest skipif when MOCK_API_TOKEN or LLM_API_KEY not in environment.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_calls_q3.py -x 2>&1 | grep -E "FAILED|ERROR|no tests ran" | head -1</automated>
  </verify>
  <done>Test file exists; tests fail because calls module missing</done>
</task>

<task type="auto">
  <name>Task 2: Implement list_calls and search_transcript tools</name>
  <files>backend/agent/tools/calls.py</files>
  <read_first>backend/agent/tools/crm.py, API.md transcript section, 02-CONTEXT.md D-09, D-10, D-11, D-12</read_first>
  <acceptance_criteria>
    - Two separate tools in one module per D-09: list_calls and search_transcript
    - list_calls: fetches /calls with filters; sorts by date desc in Python; surfaces most_recent_call_id, date, customer_id, type, outcome (D-10)
    - search_transcript: calls /calls/{id}/transcript with search param; never fetches without search when complaint extraction intended
    - Hybrid payload per D-11: complaint_type, lot_id (regex LOT-\\d{4}-\\d{4} on segment text), matched_segments capped at 20 (D-12)
    - Sources: "calls" for list; "calls/{id}/transcript" for transcript
    - Unit tests from Task 1 pass GREEN
  </acceptance_criteria>
  <action>
    Create backend/agent/tools/calls.py following crm.py pattern with two TOOL_DEFINITIONS entries.
    list_calls: accept customer_id, type, outcome, from, to filters; after fetch sort rows by call_date or date field descending; include most_recent_call_id and metadata in result JSON.
    search_transcript: require call_id and search arguments; GET /calls/{call_id}/transcript?search=...; read segments from response (not data); extract complaint_type via keyword heuristics (broken, quality, complaint); extract lot_id via regex on segment text; cap matched_segments[:20].
    run_calls_tool dispatches both tools; raise ValueError for unknown names.
    Per D-01/D-02: pre-compute complaint_type and lot_id in Python, do not leave comparison to LLM.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_calls_q3.py -k "not integration" -x -q</automated>
  </verify>
  <done>All unit tests GREEN; CALL-58020 and LOT-2026-0658 extracted correctly</done>
</task>

<task type="auto">
  <name>Task 3: Wire calls into barrel and prompts; verify Q3 E2E</name>
  <files>backend/agent/tools/__init__.py, backend/agent/prompts.py</files>
  <read_first>backend/agent/tools/__init__.py, backend/agent/prompts.py, 02-CONTEXT.md D-15</read_first>
  <acceptance_criteria>
    - Barrel includes calls tool definitions; run_tool dispatches to run_calls_tool
    - SYSTEM_PROMPT adds calls routing: list_calls first for "last call", then search_transcript with search=; never request full transcript
    - Q3 integration test passes when .env configured
    - sources includes calls and calls/CALL-58020/transcript for Q3
  </acceptance_criteria>
  <action>
    Extend backend/agent/tools/__init__.py: import calls module; append get_calls_tools() to get_tool_definitions(); add CALLS_TOOLS name set; run_tool routes to run_calls_tool.
    Extend SYSTEM_PROMPT per D-15 with calls section describing two-step workflow and transcript search constraint.
    Run full test_calls_q3.py including integration when env available.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_calls_q3.py -x -q</automated>
  </verify>
  <done>Calls vertical slice wired; Q3 unit tests pass; integration test passes with live env</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → transcript search | Untrusted search terms sent to mock API |
| Transcript segments → LLM | Potentially large text; must be capped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Denial of Service | full transcript download | mitigate | search_transcript always uses search param; cap 20 segments (D-12) |
| T-02-05 | Spoofing | call_id injection | mitigate | Validate call_id matches CALL-\\d+ pattern before API call |
| T-02-SC | Tampering | package installs | accept | No new packages in this plan |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/test_calls_q3.py -q` passes
- Two-step workflow completes within MAX_ITERATIONS=5 (list + search + answer = 3 rounds max)
- Human smoke (when .env set): curl Q3 from SAMPLE_QUESTIONS.md; answer mentions broken pasta, LOT-2026-0658, CALL-58020
</verification>

<success_criteria>
- CALLS-01 satisfied: list calls + surgical transcript search
- Q3 reference answer achievable via agent loop
- No full transcript downloads in tool implementation
</success_criteria>

<output>
Create `.planning/phases/02-all-4-verticali-kb/02-02-SUMMARY.md` when done
</output>
