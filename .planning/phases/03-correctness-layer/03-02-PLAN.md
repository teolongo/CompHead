---
phase: 03-correctness-layer
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - backend/agent/correctness.py
  - backend/agent/loop.py
  - backend/agent/prompts.py
  - backend/tests/test_phase3_traps.py
  - backend/tests/test_sample_questions.py
autonomous: true
requirements: [DATA-03, DATA-04]
must_haves:
  truths:
    - "Questions with unsupported metrics return an honest not-available answer."
    - "Questions naming a missing customer verify the CRM premise before answering."
    - "Trap answers still use HTTP 200 and the frozen /ask response schema."
  artifacts:
    - path: "backend/agent/correctness.py"
      provides: "Deterministic premise/trap preflight helpers"
    - path: "backend/agent/loop.py"
      provides: "Preflight hook before LLM tool-calling"
    - path: "backend/tests/test_phase3_traps.py"
      provides: "Unit coverage for Q7/Q8 trap behavior"
  key_links:
    - from: "backend/agent/loop.py"
      to: "backend/agent/correctness.py"
      via: "try_answer_correctness_preflight(question)"
      pattern: "try_answer_correctness_preflight"
    - from: "backend/agent/correctness.py"
      to: "/crm/customers"
      via: "premise verification search"
      pattern: "crm/customers"
---

<objective>
Create the second evaluator-visible correctness slice: Q7 and Q8 must abstain specifically and honestly instead of allowing the model to invent unavailable profit margins or nonexistent order statuses.

Purpose: DATA-03 and DATA-04 protect hidden trap questions, where precise abstention scores better than generic uncertainty or hallucinated facts.
Output: Deterministic preflight helpers, prompt guardrails, and trap tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@API.md
@SAMPLE_QUESTIONS.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-all-4-verticali-kb/02-VERIFICATION.md
@backend/agent/loop.py
@backend/agent/prompts.py
@backend/agent/tools/crm.py
@backend/services/api_client.py
@backend/tests/test_sample_questions.py

<interfaces>
`run_agent(question: str) -> dict[str, Any]` returns keys answer, sources, verticale, artifact_url and catches exceptions into HTTP-200-compatible answers.

`_infer_verticale(sources)` maps crm/, erp/, calls, and DOC- sources to the frozen verticale enum.

After Plan 03-01, `MockApiClient.get_all_pages` exists for complete CRM searches. Use it when a premise search could exceed the first page.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing trap preflight tests</name>
  <files>backend/tests/test_phase3_traps.py, backend/tests/test_sample_questions.py</files>
  <read_first>
    - backend/tests/test_sample_questions.py
    - backend/agent/loop.py
    - backend/agent/tools/crm.py
    - SAMPLE_QUESTIONS.md
  </read_first>
  <behavior>
    - Q7-style question containing `profit margin` and a `LOT-YYYY-NNNN` id returns a specific not-available answer mentioning profit or margin.
    - Q8-style question naming a missing customer searches CRM and returns that the customer is not found.
    - Preflight answers preserve `artifact_url=None`, schema keys, and relevant verticale.
  </behavior>
  <action>Create `backend/tests/test_phase3_traps.py` with mocked `get_client`/preflight dependencies so tests are fast and offline. Include one test for unsupported lot profitability fields and one test for a missing customer name such as `Supermercati Bianchi`. Keep `backend/tests/test_sample_questions.py` Q7/Q8 assertions at least as strict as they are today.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_phase3_traps.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because no `agent.correctness` preflight hook exists.
    - Tests assert the missing customer answer includes the customer name and a not-found phrase.
    - Tests assert unsupported profit margin answers do not contain numeric invented margins.
  </acceptance_criteria>
  <done>Trap behavior is encoded before production changes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic trap preflight</name>
  <files>backend/agent/correctness.py, backend/agent/loop.py, backend/tests/test_phase3_traps.py</files>
  <read_first>
    - backend/agent/loop.py
    - backend/services/api_client.py
    - backend/agent/tools/crm.py
    - backend/tests/test_phase3_traps.py
    - API.md
  </read_first>
  <action>Create `backend/agent/correctness.py` with `try_answer_correctness_preflight(question: str) -> dict[str, Any] | None`. Implement two concrete preflight branches: unsupported profitability/cost metrics for production lots, and missing-customer order-status questions. For profit margin/cost questions, identify `LOT-\d{4}-\d{4}` and return a specific answer that cost and profit margin are not stored in the available sources, with `verticale="erp"` and sources limited to any verification endpoint actually queried. For customer premise questions, extract the likely customer name, call `/crm/customers` with `search=<name>` using complete pagination, and if no customers match return `There is no customer named "<name>" in the CRM.` with `verticale="crm"` and `sources=["crm/customers"]`. In `loop.py`, call this preflight at the start of `run_agent` before LLM setup and return it immediately when non-None.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_phase3_traps.py tests/test_phase2_q1_q4.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `run_agent(Q7)` can return an honest not-available answer without relying on the LLM to infer unavailable fields.
    - `run_agent(Q8)` verifies `/crm/customers` before saying the customer does not exist.
    - All preflight returns include exactly the frozen keys expected by `AskResponse`.
    - Existing Q1-Q4 tests remain green or skip only for missing integration env.
  </acceptance_criteria>
  <done>Trap questions are intercepted deterministically and return precise abstentions.</done>
</task>

<task type="auto">
  <name>Task 3: Tighten prompt abstention rules</name>
  <files>backend/agent/prompts.py, backend/tests/test_phase3_traps.py</files>
  <read_first>
    - backend/agent/prompts.py
    - AGENTS.md
    - SAMPLE_QUESTIONS.md
    - backend/tests/test_phase3_traps.py
  </read_first>
  <action>Update `SYSTEM_PROMPT` with explicit correctness rules: verify named entities with the appropriate list/search tool before answering; never infer profit margin, cost, or unavailable financial metrics from orders/lots/inventory; when a verified premise is missing, answer specifically that the entity was not found in the source. Keep the prompt short enough for latency and preserve the final `submit_answer` instruction.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_phase3_traps.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Prompt contains concrete "profit margin/cost not available unless a tool returns it" guidance.
    - Prompt contains concrete "verify named customers before order/status answers" guidance.
    - No prompt text asks for external data, schema changes, streaming, auth, or hidden fallback data.
  </acceptance_criteria>
  <done>LLM behavior is aligned with deterministic trap handling for hidden variants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evaluator to preflight | Untrusted free-text questions are parsed before the LLM loop |
| preflight to CRM API | Extracted customer names become outbound query parameters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | `correctness.py` parsing | mitigate | Use narrow regexes and exact unsupported-metric keywords; return None for ambiguous questions |
| T-03-05 | Repudiation | trap abstentions | mitigate | Include sources for premise checks and phrase answers with the verified missing premise |
| T-03-06 | Information Disclosure | prompt rules | accept | Prompt contains no secrets and only challenge rules |
| T-03-SC | Tampering | package installs | accept | No package-manager installs in this plan |
</threat_model>

<verification>
Run `cd backend && uv run pytest tests/test_phase3_traps.py tests/test_phase2_q1_q4.py -q`.
If live env is configured, run Q7 and Q8 integration cases: `cd backend && uv run pytest tests/test_sample_questions.py::test_sample_question[q07_erp_trap_profit_margin] tests/test_sample_questions.py::test_sample_question[q08_crm_trap_missing_customer] -q`.
</verification>

<success_criteria>
- DATA-03: Missing-entity questions verify premises before answering.
- DATA-04: Unavailable metrics produce specific not-available answers.
- `/ask` remains public, non-streaming, HTTP 200, and schema-compatible.
</success_criteria>

<output>
Create `.planning/phases/03-correctness-layer/03-02-SUMMARY.md` when done.
</output>
