---
phase: 03-correctness-layer
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - backend/tests/test_phase3_contract.py
  - backend/tests/test_sample_questions.py
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, OPS-01, OPS-02]
user_setup:
  - service: railway
    why: "First public deploy and endpoint contract check"
    env_vars:
      - name: LLM_BASE_URL
        source: "Local backend/.env or provider dashboard"
      - name: LLM_API_KEY
        source: "LLM provider dashboard"
      - name: MODEL
        source: "LLM provider model id"
      - name: MOCK_API_BASE_URL
        source: "https://aldente.yellowtest.it"
      - name: MOCK_API_TOKEN
        source: "Challenge platform dashboard"
      - name: PUBLIC_BASE_URL
        source: "Railway generated domain"
    dashboard_config:
      - task: "Run the platform endpoint check after deploy"
        location: "Challenge platform dashboard"
must_haves:
  truths:
    - "Sample questions 5-8 and 10-12 pass locally or report only provider rate-limit retry exhaustion."
    - "The public /ask endpoint keeps the frozen schema and always returns HTTP 200."
    - "Railway service deploys in EU with `europe-west4` scaling confirmed or set, then passes the platform endpoint check."
  artifacts:
    - path: "backend/tests/test_phase3_contract.py"
      provides: "FastAPI contract tests for /ask shape and HTTP 200 abstentions"
    - path: "backend/tests/test_sample_questions.py"
      provides: "Regression selection for Phase 3 sample questions"
  key_links:
    - from: "Railway public URL"
      to: "/ask"
      via: "RAILWAY_URL/PUBLIC_BASE_URL variable, curl, and platform endpoint check"
      pattern: "POST /ask"
    - from: "Railway service"
      to: "EU region"
      via: "DEPLOY.md `railway scale --service <name> --europe-west4=1` recovery path"
      pattern: "europe-west4"
    - from: "Phase 3 data helpers"
      to: "sample regression battery"
      via: "Q5-Q8 and Q10-Q12 integration cases"
      pattern: "test_sample_question"
---

<objective>
Create the final Phase 3 readiness slice: verify the correctness layer locally, prove the `/ask` contract with FastAPI tests, deploy to Railway, and run the platform endpoint check.

Purpose: OPS-01 and OPS-02 are required before submission, and they must happen after the data correctness paths are in place.
Output: Contract tests, focused sample-question regression command, Railway deploy, public smoke test, and human endpoint-check confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@DEPLOY.md
@SAMPLE_QUESTIONS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@backend/main.py
@backend/agent/loop.py
@backend/tests/conftest.py
@backend/tests/test_sample_questions.py
@backend/railway.json
@backend/pyproject.toml

<interfaces>
FastAPI route: `@app.post("/ask", response_model=AskResponse)` accepts `AskRequest(question: str)` and returns `AskResponse(answer, sources, verticale, artifact_url=None)`.

Railway start command: `uv run uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}`. Healthcheck path is `/health`.

DEPLOY.md says to set Railway Account Settings -> Default Region -> EU West Metal (Amsterdam) before project creation, and to move a US service with `railway scale --service <name> --europe-west4=1`.

Integration tests are marked `@pytest.mark.integration` and skip when `MOCK_API_TOKEN` or `LLM_API_KEY` is absent. `tests/conftest.py` spaces integration tests to reduce provider 429s.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add /ask contract and abstention tests</name>
  <files>backend/tests/test_phase3_contract.py</files>
  <read_first>
    - backend/main.py
    - backend/agent/loop.py
    - AGENTS.md
    - backend/tests/conftest.py
  </read_first>
  <behavior>
    - POST /ask accepts only `{"question": str}` as required input and returns JSON keys answer, sources, verticale, artifact_url.
    - When `run_agent` returns an abstention or raises, the HTTP status is still 200.
    - Response never streams and never requires auth headers.
  </behavior>
  <action>Create `backend/tests/test_phase3_contract.py` using FastAPI `TestClient` and monkeypatching `main.run_agent` where needed. Add tests for a normal frozen-schema response, an honest not-available response, and an exception fallback. Do not add auth, alternate paths, streaming, jobs, or required headers.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_phase3_contract.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Contract tests assert status code 200 for normal and fallback responses.
    - Contract tests assert response keys include `answer`, `sources`, `verticale`, and `artifact_url`.
    - Contract tests do not call live LLM or mock API services.
  </acceptance_criteria>
  <done>Frozen endpoint behavior is covered before deploy.</done>
</task>

<task type="auto">
  <name>Task 2: Run focused Phase 3 regression battery</name>
  <files>backend/tests/test_sample_questions.py</files>
  <read_first>
    - backend/tests/test_sample_questions.py
    - backend/tests/test_phase3_pagination.py
    - backend/tests/test_phase3_traps.py
    - backend/tests/test_phase3_multihop.py
    - SAMPLE_QUESTIONS.md
  </read_first>
  <action>Run the offline Phase 3 tests first, then run the live integration cases for Q5, Q6, Q7, Q8, Q10, Q11, and Q12 with the existing retry and pacing environment. If provider 429s persist after retries, report that exact limitation in the SUMMARY and re-run the individual failed case after a cooldown. Do not weaken assertions to pass a bad answer.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_phase3_contract.py tests/test_phase3_pagination.py tests/test_phase3_traps.py tests/test_phase3_multihop.py -q</automated>
    <automated>cd backend && uv run pytest tests/test_sample_questions.py::test_sample_question[q05_calls_kb_return_policy] tests/test_sample_questions.py::test_sample_question[q06_crm_negotiation_by_channel] tests/test_sample_questions.py::test_sample_question[q07_erp_trap_profit_margin] tests/test_sample_questions.py::test_sample_question[q08_crm_trap_missing_customer] tests/test_sample_questions.py::test_sample_question[q10_erp_bom_supplier_stock] tests/test_sample_questions.py::test_sample_question[q11_calls_broken_pasta_count] tests/test_sample_questions.py::test_sample_question[q12_kb_price_authority] -q</automated>
  </verify>
  <acceptance_criteria>
    - Offline Phase 3 tests pass.
    - Live sample tests Q5-Q8 and Q10-Q12 pass when `.env` has provider and mock API keys, or the SUMMARY records only rate-limit exhaustion with retry evidence.
    - No test is changed to accept hallucinated values, missing sources, or wrong verticale except the existing flexible Q7 trap behavior.
  </acceptance_criteria>
  <done>Local regression evidence is ready for deployment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deploy to Railway and pass endpoint check</name>
  <files>backend/railway.json, backend/pyproject.toml, DEPLOY.md</files>
  <read_first>
    - DEPLOY.md
    - backend/railway.json
    - backend/pyproject.toml
    - backend/main.py
  </read_first>
  <what-built>The executor should automate Railway CLI checks, deploy from `backend/`, set required variables when available, confirm or set EU `europe-west4` service scaling, generate or read the Railway domain into `RAILWAY_URL`/`PUBLIC_BASE_URL`, and smoke-test `/health` plus `/ask` before this checkpoint.</what-built>
  <action>Verify `railway --version`; if not installed, stop with install instructions from DEPLOY.md. From `backend/`, run `railway status` and identify the linked service name; if no project is linked, ask the human to confirm the Railway account default region is EU West Metal (Amsterdam) per DEPLOY.md, then run `railway init` and `railway up`. If a service already exists or the region is unclear, set EU scaling explicitly with `railway scale --service "$RAILWAY_SERVICE" --europe-west4=1`, then run `railway scale --service "$RAILWAY_SERVICE"` and confirm `europe-west4` is present. Ensure Railway variables include `LLM_BASE_URL`, `LLM_API_KEY`, `MODEL`, `MOCK_API_BASE_URL`, and `MOCK_API_TOKEN`; never print or hardcode secret values. Generate/read the public domain with `railway domain`, assign it to `RAILWAY_URL`, set `PUBLIC_BASE_URL` to that same absolute URL with `railway variables --set PUBLIC_BASE_URL="$RAILWAY_URL"`, and use that variable for all curl checks. Smoke test `GET "$RAILWAY_URL/health"` expecting `{"status":"ok"}` and `POST "$RAILWAY_URL/ask"` with a simple question expecting HTTP 200 JSON with the frozen schema. Ask the human to run the challenge platform endpoint check because that dashboard is not available through CLI.</action>
  <how-to-verify>
    1. Open the challenge platform dashboard endpoint check.
    2. Submit the Railway public base URL, not `/ask` appended unless the dashboard asks for it.
    3. Confirm the endpoint check passes schema, no-auth, non-streaming, HTTP 200, and latency checks.
  </how-to-verify>
  <resume-signal>Type `endpoint check passed` with the Railway URL, or paste the endpoint-check failure details.</resume-signal>
  <verify>
    <automated>cd backend && railway --version && railway status</automated>
    <automated>cd backend && RAILWAY_SERVICE="${RAILWAY_SERVICE:?set this to the linked Railway service name from railway status}" && railway scale --service "$RAILWAY_SERVICE" --europe-west4=1</automated>
    <automated>cd backend && RAILWAY_SERVICE="${RAILWAY_SERVICE:?set this to the linked Railway service name from railway status}" && railway scale --service "$RAILWAY_SERVICE"</automated>
    <automated>cd backend && RAILWAY_URL="${PUBLIC_BASE_URL:-$(railway domain | sed -n 's#.*\(https://[^[:space:]]*\).*#\1#p' | head -1)}" && test -n "$RAILWAY_URL" && railway variables --set PUBLIC_BASE_URL="$RAILWAY_URL" && curl -fsS "$RAILWAY_URL/health"</automated>
    <automated>cd backend && RAILWAY_URL="${PUBLIC_BASE_URL:-$(railway domain | sed -n 's#.*\(https://[^[:space:]]*\).*#\1#p' | head -1)}" && test -n "$RAILWAY_URL" && curl -fsS -X POST "$RAILWAY_URL/ask" -H 'Content-Type: application/json' -d '{"question":"What is the status of the order for Supermercati Bianchi?"}'</automated>
    <human-check>Challenge platform endpoint check reports pass.</human-check>
  </verify>
  <acceptance_criteria>
    - Railway service is deployed in an EU region, with `europe-west4` scaling confirmed or explicitly set via Railway CLI.
    - Public `/health` returns `{"status":"ok"}`.
    - Public `/ask` returns HTTP 200 JSON with frozen keys and no auth.
    - `PUBLIC_BASE_URL` equals the Railway public base URL used for smoke tests.
    - Platform endpoint check passes before Phase 3 is marked complete.
  </acceptance_criteria>
  <done>First Railway deploy is live and the evaluator contract is confirmed by the platform endpoint check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet to /ask | Evaluator and users can call `/ask` without auth by design |
| Railway runtime to env vars | Secrets are supplied by Railway variables and must not be committed |
| human dashboard to endpoint check | Platform endpoint check result is external evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Spoofing | `/ask` endpoint | accept | Challenge requires public no-auth endpoint |
| T-03-11 | Information Disclosure | Railway variables | mitigate | Set secrets via Railway variables only; never commit `.env` or hardcode keys |
| T-03-12 | Denial of Service | public endpoint latency | mitigate | Run focused regression and smoke tests; keep deterministic preflight fast |
| T-03-SC | Tampering | package installs | accept | No package-manager installs in this plan |
</threat_model>

<verification>
Offline: `cd backend && uv run pytest tests/test_phase3_contract.py tests/test_phase3_pagination.py tests/test_phase3_traps.py tests/test_phase3_multihop.py -q`.
Live local: run Q5-Q8 and Q10-Q12 sample cases from `tests/test_sample_questions.py` with env configured and integration pacing.
Deploy: Railway EU `europe-west4` scaling is confirmed or set, `PUBLIC_BASE_URL` is populated from the Railway public domain, `/health` and `/ask` smoke tests pass using `RAILWAY_URL`, then the platform endpoint check passes.
</verification>

<success_criteria>
- DATA-01..05: The focused Phase 3 sample regression battery passes locally.
- OPS-01: Railway deployment exists in the intended EU region with `europe-west4` scaling evidence.
- OPS-02: Platform endpoint check passes against the Railway URL.
</success_criteria>

<output>
Create `.planning/phases/03-correctness-layer/03-04-SUMMARY.md` when done.
</output>
