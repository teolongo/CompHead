---
phase: 03-correctness-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/api_client.py
  - backend/agent/tools/crm.py
  - backend/agent/tools/calls.py
  - backend/tests/test_phase3_pagination.py
  - backend/tests/test_sample_questions.py
autonomous: true
requirements: [DATA-01, DATA-02]
must_haves:
  truths:
    - "Aggregation answers are based on every API row, not the default first page."
    - "CRM stage totals are grouped by customer channel with Python arithmetic."
    - "Broken pasta complaint counts page through all calls and compute the exact count in code."
  artifacts:
    - path: "backend/services/api_client.py"
      provides: "Shared pagination helper that follows pagination.total"
    - path: "backend/agent/tools/crm.py"
      provides: "Complete opportunity aggregation and channel grouping"
    - path: "backend/agent/tools/calls.py"
      provides: "Complete call-log pagination and defect counting"
    - path: "backend/tests/test_phase3_pagination.py"
      provides: "Unit coverage for multi-page aggregation"
  key_links:
    - from: "backend/agent/tools/crm.py"
      to: "backend/services/api_client.py"
      via: "get_all_pages for /crm/opportunities and customer lookups"
      pattern: "get_all_pages"
    - from: "backend/agent/tools/calls.py"
      to: "/calls/{id}/transcript?search="
      via: "targeted transcript searches only"
      pattern: "transcript"
---

<objective>
Create the first Phase 3 evaluator-visible correctness slice: Q6 and Q11 must use complete paginated source data and Python-side arithmetic instead of first-page samples or LLM math.

Purpose: DATA-01 and DATA-02 are the most common hidden-eval failure mode; every aggregate must check pagination.total and page until all rows are available.
Output: Shared pagination helper, upgraded CRM/calls aggregate tools, and unit tests proving multi-page totals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@API.md
@SAMPLE_QUESTIONS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-all-4-verticali-kb/02-04-SUMMARY.md
@backend/services/api_client.py
@backend/agent/tools/crm.py
@backend/agent/tools/calls.py
@backend/tests/test_sample_questions.py

<interfaces>
MockApiClient currently exposes get(path, params=None) and returns the API JSON envelope. List endpoints return {"data": [...], "pagination": {"offset", "limit", "total"}} with max limit 200.

CRM tool pattern: TOOL_DEFINITIONS, get_tool_definitions(), private _run_list_* handlers, run_crm_tool(name, arguments) returning (json_string, source).

Calls tool pattern: list_calls currently returns most_recent_call_id and calls_sample; search_transcript must keep using /calls/{call_id}/transcript with a search parameter and must not download full transcripts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing multi-page aggregate tests</name>
  <files>backend/tests/test_phase3_pagination.py, backend/tests/test_sample_questions.py</files>
  <read_first>
    - backend/tests/test_phase2_q1_q4.py
    - backend/tests/test_sample_questions.py
    - backend/agent/tools/crm.py
    - backend/agent/tools/calls.py
  </read_first>
  <behavior>
    - CRM test: mocked /crm/opportunities returns two pages and the final result totals all negotiation opportunities, not only page 1.
    - CRM test: group_by_channel result contains GDO, distributor, and horeca buckets with Python-computed totals.
    - Calls test: mocked /calls returns two pages and the defect counter searches only targeted transcript snippets, producing count 9 for broken pasta.
  </behavior>
  <action>Create `backend/tests/test_phase3_pagination.py` with mocked clients that assert `limit=200`, advancing `offset`, and reading `pagination.total` until complete. Extend existing sample tests only if needed to isolate Q6 and Q11 with ids `q06_crm_negotiation_by_channel` and `q11_calls_broken_pasta_count`; do not weaken existing assertions. Run the new tests first and confirm they fail for missing pagination or missing aggregate behavior.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_phase3_pagination.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The new unit tests fail before implementation because only the first page is used or the requested aggregate shape is absent.
    - Tests assert API call params include `limit=200` and the second page `offset`.
    - Tests do not require live `MOCK_API_TOKEN` or `LLM_API_KEY`.
  </acceptance_criteria>
  <done>Failing tests encode complete pagination, channel grouping, and broken-pasta defect counting behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Implement shared pagination and CRM channel totals</name>
  <files>backend/services/api_client.py, backend/agent/tools/crm.py, backend/tests/test_phase3_pagination.py</files>
  <read_first>
    - backend/services/api_client.py
    - backend/agent/tools/crm.py
    - API.md
    - backend/tests/test_phase3_pagination.py
  </read_first>
  <action>Add `MockApiClient.get_all_pages(path: str, params: dict | None = None, limit: int = 200, data_key: str = "data") -> list[dict]` that copies params, sends `limit` and `offset`, reads `pagination.total`, and stops only after collected rows reach total or a page returns no rows. Update `crm.py` so aggregate paths use `get_all_pages` for `/crm/opportunities` and any needed `/crm/customers` lookups. Extend `list_opportunities` with concrete optional arguments `group_by` accepting `customer_channel` and `stage`; when `group_by="customer_channel"` and `stage="negotiation"`, compute `grouped_total_value_eur` in Python for channels `GDO`, `distributor`, and `horeca`, using opportunity `customer_channel`/`channel` fields when present and otherwise targeted customer lookups by `customer_id`. Preserve existing Q1 behavior for customer-specific open opportunities.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_phase3_pagination.py tests/test_phase2_q1_q4.py::test_list_opportunities_cust_0132_open_stats -q</automated>
  </verify>
  <acceptance_criteria>
    - `MockApiClient.get_all_pages` follows `pagination.total` and never mutates the caller's params dict.
    - Existing `list_opportunities({"customer_id": "CUST-0132"})` still returns count 4 and total 740000 in the Phase 2 unit test.
    - New CRM pagination tests pass and assert all values are summed in Python.
    - No `/ask` schema, auth behavior, or response model changes are made.
  </acceptance_criteria>
  <done>CRM aggregate questions have complete-row totals and can answer Q6's channel grouping from tool output.</done>
</task>

<task type="auto">
  <name>Task 3: Implement paginated broken-pasta complaint count</name>
  <files>backend/agent/tools/calls.py, backend/tests/test_phase3_pagination.py, backend/tests/test_sample_questions.py</files>
  <read_first>
    - backend/agent/tools/calls.py
    - API.md
    - SAMPLE_QUESTIONS.md
    - backend/tests/test_phase3_pagination.py
  </read_first>
  <action>Add a calls tool named `count_calls_by_defect` with arguments `defect` and optional `outcome`. It must call `get_all_pages("/calls", params)` with targeted filters where available, then call `/calls/{call_id}/transcript` with `search=<defect>` for candidate calls only. Count a call once when matched transcript segments contain the defect phrase, return `defect`, `count`, `searched_call_count`, and a small `matching_call_ids_sample`. Keep `search_transcript` unchanged for last-call questions and keep transcript result caps. Update tool definitions so the LLM has a direct path for Q11 instead of iterating manually.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_phase3_pagination.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `count_calls_by_defect({"defect": "broken pasta"})` uses complete paginated `/calls` results.
    - Transcript calls include a `search` param and never request full transcripts.
    - The mocked Q11 unit test returns exact count 9 from Python code.
    - `backend/tests/test_sample_questions.py` still expects Q11 to include 9 and calls sources.
  </acceptance_criteria>
  <done>Q11 has a deterministic tool path that pages all 80 calls and counts broken-pasta complaints accurately.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evaluator to /ask | Untrusted natural-language questions influence which tool arguments the LLM emits |
| backend to mock API | Authenticated outbound requests consume metered API quota |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | `get_all_pages` | mitigate | Cap per-page limit at 200, stop on `pagination.total`, and stop on empty pages to avoid infinite loops |
| T-03-02 | Tampering | aggregate arithmetic | mitigate | Compute sums/counts in Python from API rows; tests assert exact totals |
| T-03-03 | Information Disclosure | transcript search | mitigate | Use `search` transcript endpoint and cap returned samples instead of fetching full transcripts |
| T-03-SC | Tampering | package installs | accept | No new package-manager installs in this plan |
</threat_model>

<verification>
Run `cd backend && uv run pytest tests/test_phase3_pagination.py tests/test_phase2_q1_q4.py::test_list_opportunities_cust_0132_open_stats -q`.
If live env is configured and rate limits allow, run `cd backend && uv run pytest tests/test_sample_questions.py::test_sample_question[q06_crm_negotiation_by_channel] tests/test_sample_questions.py::test_sample_question[q11_calls_broken_pasta_count] -q`.
</verification>

<success_criteria>
- DATA-01: All list aggregate helpers check `pagination.total` and request subsequent pages.
- DATA-02: Q6 channel totals and Q11 defect count are computed in Python.
- Existing Q1 and Q3 tool behavior remains intact.
</success_criteria>

<output>
Create `.planning/phases/03-correctness-layer/03-01-SUMMARY.md` when done.
</output>
