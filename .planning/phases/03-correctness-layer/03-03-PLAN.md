---
phase: 03-correctness-layer
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - backend/agent/correctness.py
  - backend/agent/loop.py
  - backend/agent/prompts.py
  - backend/agent/tools/erp.py
  - backend/tests/test_phase3_multihop.py
  - backend/tests/test_sample_questions.py
autonomous: true
requirements: [DATA-02, DATA-03, DATA-04, DATA-05]
must_haves:
  truths:
    - "Return-policy questions combine last-call complaint facts with the KB quality policy."
    - "DATA-05 lot-shaped BOM questions resolve LOT-... to finished SKU, then traverse BOM to raw material to supplier to raw-material stock."
    - "Official KB price-list facts override conflicting call transcript facts."
  artifacts:
    - path: "backend/agent/correctness.py"
      provides: "Deterministic multi-hop answer helpers for sample-shaped chains"
    - path: "backend/agent/tools/erp.py"
      provides: "Lot-to-SKU-to-BOM/supplier/inventory chain output for semolina questions"
    - path: "backend/tests/test_phase3_multihop.py"
      provides: "Unit coverage for Q5, LOT-shaped Q10/DATA-05, and Q12-shaped chains"
  key_links:
    - from: "last NordSpesa call"
      to: "KB quality policy"
      via: "search_transcript then search_kb"
      pattern: "return policy"
    - from: "LOT-... production/order/shipment data"
      to: "finished SKU such as PAS-SPA-500"
      via: "production order, order, or shipment lot resolution"
      pattern: "LOT-"
    - from: "finished SKU such as PAS-SPA-500"
      to: "RAW-SEM-003 supplier and inventory"
      via: "BOM, supplier, inventory"
      pattern: "RAW-SEM"
    - from: "PAS-FUS-500 price"
      to: "DOC-015 official price list"
      via: "KB authoritative source"
      pattern: "DOC-015"
---

<objective>
Create the third evaluator-visible correctness slice: multi-hop questions must resolve facts across sources deterministically enough to pass Q5, Q10, and Q12 and generalize to hidden variants with the same shapes.

Purpose: DATA-05 requires chaining source facts rather than asking the model to infer missing links from partial tool samples.
Output: Multi-hop helpers, ERP lot-to-SKU chain output, prompt routing, and tests for return policy, BOM supplier stock, and authoritative price-list behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@API.md
@SAMPLE_QUESTIONS.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-all-4-verticali-kb/02-01-SUMMARY.md
@.planning/phases/02-all-4-verticali-kb/02-02-SUMMARY.md
@.planning/phases/02-all-4-verticali-kb/02-03-SUMMARY.md
@backend/agent/correctness.py
@backend/agent/loop.py
@backend/agent/tools/erp.py
@backend/agent/tools/calls.py
@backend/agent/tools/kb.py
@backend/tests/test_sample_questions.py

<interfaces>
After Plan 03-02, `try_answer_correctness_preflight(question)` returns a frozen-schema dict or None and is called at the start of `run_agent`.

ERP tools currently include `get_inventory`, `list_bom`, `list_suppliers`, `list_production_orders`, and `list_shipments`. Production orders and shipments can expose production lot IDs and finished SKUs; BOM endpoint filters by finished SKU; inventory endpoint filters by `search` and `type`; suppliers endpoint filters by `search` or `category`.

KB retrieval returns whole documents and DOC-### sources. `search_kb` should be used for quality policy and official price-list facts, and official documents are authoritative when calls disagree.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing multi-hop chain tests</name>
  <files>backend/tests/test_phase3_multihop.py, backend/tests/test_sample_questions.py</files>
  <read_first>
    - backend/tests/test_sample_questions.py
    - backend/agent/correctness.py
    - backend/agent/tools/erp.py
    - SAMPLE_QUESTIONS.md
  </read_first>
  <behavior>
    - Q5-style return policy answer includes broken pasta, replacement or credit note, and a DOC source.
    - DATA-05 LOT-shaped BOM answer accepts a `LOT-...` question, resolves the lot to its finished SKU, then includes RAW-SEM-003, Molino San Giorgio, and below-minimum status from inventory.
    - Q12-style price authority answer includes 8.07 EUR, DOC-015, and says the official price list wins over a call mention.
  </behavior>
  <action>Create `backend/tests/test_phase3_multihop.py` with offline mocked tool/API/KB data for Q5, Q12, and DATA-05 lot-shaped Q10 variants. Include a test question using a `LOT-2026-....` style ID that proves the helper resolves `lot_id` to a finished SKU before BOM/supplier/inventory lookup; do not let the test pass by providing the finished SKU directly. Tests should call either the new deterministic helper functions directly or `run_agent` with mocked preflight dependencies; choose the approach that best isolates logic without live LLM calls. Keep existing integration assertions in `backend/tests/test_sample_questions.py` unchanged or stricter.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_phase3_multihop.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation because chain helpers do not exist or do not return complete facts.
    - Tests assert sources include both calls/KB for Q5, ERP for Q10, and DOC-015 for Q12.
    - Tests assert DATA-05 coverage by verifying a `LOT-...` input triggers lot-to-finished-SKU resolution before BOM traversal.
    - Tests assert arithmetic/comparison fields are consumed from tool output, not recomputed by the LLM.
  </acceptance_criteria>
  <done>Multi-hop correctness expectations are executable and offline.</done>
</task>

<task type="auto">
  <name>Task 2: Add ERP lot-to-BOM supplier stock chain</name>
  <files>backend/agent/tools/erp.py, backend/tests/test_phase3_multihop.py</files>
  <read_first>
    - backend/agent/tools/erp.py
    - backend/services/api_client.py
    - API.md
    - backend/tests/test_phase3_multihop.py
  </read_first>
  <action>Add an ERP tool named `resolve_bom_supplier_stock` with optional `sku`, optional `lot_id`, and optional `material_category`, requiring at least one of `sku` or `lot_id`. When `lot_id` is provided, resolve it to the finished SKU first by searching targeted production-order data for that lot; if the lot is only reachable through order/shipment data in the current API shape, follow the production/order/shipment fields needed to recover the finished SKU without bulk-downloading unrelated records. After the finished SKU is known, fetch the BOM, select the semolina/raw-material row when the question asks for semolina, identify the raw material SKU, resolve the supplier from fields on the BOM row when present or targeted `/erp/suppliers` lookup when needed, then call `get_inventory`/`/erp/inventory` for the raw material SKU. Return `lot_id`, `finished_sku`, `raw_material_sku`, `raw_material_name`, `supplier_id`, `supplier_name`, `below_minimum`, `on_hand_qty`, and `minimum_qty`. Keep existing `get_inventory` and `list_bom` behavior intact.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_phase3_multihop.py tests/test_erp_q2.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Q10 mocked chain accepts a `LOT-...` input, resolves it to the expected finished SKU, then returns RAW-SEM-003, Molino San Giorgio, and not-below-minimum status.
    - The tool uses filtered calls: targeted production/order/shipment lookup for the lot, `/erp/bom?sku=...`, supplier lookup only when needed, and `/erp/inventory?search=RAW-SEM-003&type=raw_material`.
    - Existing Q2 inventory tests still pass.
  </acceptance_criteria>
  <done>Q10 and hidden DATA-05 lot-shaped questions have one deterministic ERP tool output with all facts needed for the final answer.</done>
</task>

<task type="auto">
  <name>Task 3: Implement return-policy and price-authority preflight chains</name>
  <files>backend/agent/correctness.py, backend/agent/loop.py, backend/agent/prompts.py, backend/tests/test_phase3_multihop.py</files>
  <read_first>
    - backend/agent/correctness.py
    - backend/agent/loop.py
    - backend/agent/prompts.py
    - backend/agent/tools/calls.py
    - backend/agent/tools/kb.py
    - backend/tests/test_phase3_multihop.py
  </read_first>
  <action>Extend `try_answer_correctness_preflight` with two multi-hop branches. For return-policy questions that reference a last complaint, resolve the customer with CRM when named, get the latest call via calls tooling, search its transcript for the complaint defect and lot, search KB for `quality returns policy broken pasta`, and return whether it qualifies using policy facts only. For price-authority questions that mention a SKU and disagreement between a call and official price list, search KB for the official 2026 wholesale price list and SKU, extract the list price from the official document, and state that the official KB document is authoritative over call notes. Update `SYSTEM_PROMPT` to prefer `resolve_bom_supplier_stock` for BOM/supplier/stock chains; when a question contains a `LOT-...` identifier, pass `lot_id` rather than requiring the model to infer a finished SKU. Also update the prompt to use official KB documents over calls for price conflicts.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_phase3_multihop.py tests/test_phase3_traps.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Q5 preflight answer includes complaint defect, lot, policy qualification, and replacement or credit note.
    - Q12 preflight answer includes 8.07 EUR, `DOC-015`, and authoritative official price-list rationale.
    - Existing trap preflight behavior from Plan 03-02 remains green.
    - The loop still returns preflight answers before creating an LLM client, reducing latency for these shapes.
  </acceptance_criteria>
  <done>Q5 and Q12 have deterministic multi-source paths, and Q10 has a direct ERP tool path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evaluator to chain parser | Untrusted question text is classified into supported chain shapes |
| backend to mock API and KB files | Chain helpers combine metered API facts with local documents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | multi-hop answer composition | mitigate | Tests assert exact linked facts and source IDs; official KB price-list source wins conflicts |
| T-03-08 | Denial of Service | return-policy call chain | mitigate | Use filtered list calls and transcript `search`; do not iterate full transcripts |
| T-03-09 | Spoofing | supplier/material resolution | mitigate | Resolve supplier from BOM/API fields or targeted supplier lookup, never from model inference |
| T-03-SC | Tampering | package installs | accept | No package-manager installs in this plan |
</threat_model>

<verification>
Run `cd backend && uv run pytest tests/test_phase3_multihop.py tests/test_phase3_traps.py tests/test_erp_q2.py -q`.
If live env is configured, run `cd backend && uv run pytest tests/test_sample_questions.py::test_sample_question[q05_calls_kb_return_policy] tests/test_sample_questions.py::test_sample_question[q10_erp_bom_supplier_stock] tests/test_sample_questions.py::test_sample_question[q12_kb_price_authority] -q`.
</verification>

<success_criteria>
- DATA-05: Q5, LOT-shaped Q10, and Q12-shaped multi-hop chains return linked facts across sources; ERP lot questions resolve lot -> finished SKU -> BOM -> supplier -> stock.
- DATA-02: Stock comparisons and price/list facts are deterministic source-derived values.
- DATA-03/DATA-04: If any chain premise is missing, the helper returns a specific not-found/not-available answer rather than inventing.
</success_criteria>

<output>
Create `.planning/phases/03-correctness-layer/03-03-SUMMARY.md` when done.
</output>
